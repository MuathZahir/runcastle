packages:
  - 'packages/*'

# pnpm 10 blocks dependency build scripts unless they are named here. esbuild's
# is what materializes the platform binary vitest transpiles with.
onlyBuiltDependencies:
  - esbuild
