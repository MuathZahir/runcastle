{
  "name": "@burn-cache-fixture/lib",
  "version": "0.0.0",
  "private": true,
  "main": "src/index.ts",
  "types": "dist/index.d.ts",
  "scripts": {
    "typecheck": "tsc -b",
    "test": "vitest run",
    "build": "mkdir -p .out && cp package.json .out/bundle.json"
  },
  "devDependencies": {
    "typescript": "5.9.3",
    "vitest": "3.2.7"
  }
}
