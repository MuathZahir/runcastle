// @runcastle/server — boot entry (SPEC §3, owner A1).
//
// TODO(A1): Hono app; mount /api/trpc, /api/hooks, /mcp; start listener on
// config.serverPort (4512); run db migrate (drizzle-kit push). Wire services,
// tRPC routers, launcher, MCP server, and the workflow runner. See docs/SPEC.md
// §3–§8 for the full file-ownership map and contracts.

export {}
