# Test notes

## Lap 1

- [ ] [Code review — Standards axis] The burn-time copy command hand-builds a Windows path with a literal separator. packages/server/src/workflows/ticket-burner.ts, in buildAttachmentCopyCommands: `copy /Y "${src}" "${dir}\\${basename(src)}" >nul`. CLAUDE.md § Conventions states "Windows paths: always node:path (join, resolve); never hand-concatenate; quote paths in shell commands". The quoting half of the rule is honoured; the concatenation half is not — the destination is assembled with a hardcoded `\\` rather than `join(dir, basename(src))`. Judgement call, not a hard break: the string is a cmd.exe argument and the test at packages/server/test/burn-attachments.test.ts ("copies with the shell each host actually has") asserts this exact spelling, so it works today. Expected: the destination built with node:path like every other path in the codebase.
- [ ] [Code review — Spec axis] The burn-time screenshot never reaches the agent under the default Windows/macOS burn mode. spec.md "Riding into the burn" says "Copy-at-burn works identically under docker and noSandbox". It does not. Verified chain: (1) config.ts defaults sandbox='docker' and burnWorkspace='auto'; (2) resolveBurnWorkspaceMode (ticket-burner.ts ~511) returns `platform === 'linux' ? 'mounted' : 'isolated'` for 'auto' — so on this Windows host the default is 'isolated'; (3) the attachment copy is a hooks.host.onWorktreeReady command with cwd = the host burn worktree, i.e. the MOUNTED workspace (/home/agent/workspace); (4) buildIsolatedSetupCommand (~578) then runs `git clone /home/agent/workspace /home/agent/repo`, and buildWorkspaceNotes (~616) tells the agent "Do ALL work there: cd /home/agent/repo first". A git clone carries tracked content only, and the PNG is untracked AND listed in .git/info/exclude by design — so it cannot arrive in the clone. The ticket context still says "An annotated screenshot of the problem is at .runcastle-attachments/<noteId>.png in your workspace — Read it before starting", and that relative path resolves to nothing from /home/agent/repo. Expected: the agent can Read the screenshot on a default docker burn. Actual: the Read fails on every Windows and macOS docker burn; only a Linux host (mounted) or an explicit burnWorkspace='mounted' / sandbox='noSandbox' works. This is not the graceful degradation the spec allows for — that is scoped to "A missing PNG at burn time"; here the PNG exists on the host and the context names a path the agent cannot reach. packages/server/test/burn-attachments.test.ts only exercises the mounted worktree, so the gap is invisible to the suite.
- [ ] [Code review — Spec axis] The attachment exclude writes a permanent, never-removed line into the human's own repo. spec.md "Riding into the burn" says only "The attachment directory is cleaned out of the workspace before commit/merge so it never lands in the repo." The implementation adds a second, stickier mechanism: excludePath (packages/server/src/services/git.ts ~707) appends `.runcastle-attachments/` to the project repo's .git/info/exclude before the first copy, and nothing ever removes it. Because git resolves info/exclude against the COMMON git dir, that one line applies to the parent checkout too — so from the first annotated-note burn onward, any directory named .runcastle-attachments/ in the human's own working tree is silently invisible to their `git status`, forever, in every worktree. Reproduce: promote an annotated note, burn it, then `cat .git/info/exclude` — the line is there after the run completes. Expected (from the spec wording): a cleanup confined to the burn workspace. Actual: a permanent per-clone git config change. The directory-clearing half (clearAttachments) also runs after run() returns rather than "before commit" — before merge, so the merge is safe, but the agent's own commits during the run are covered only by the exclude, not by the clean.
- [ ] [Code review — Standards axis] The annotation pen introduces a red outside the pinned palette. apps/web/src/lib/walkthrough.ts ~30: `export const STROKE_COLOR = '#ff2b2b'`. docs/UI-SPEC.md line 58 pins the palette exhaustively, including the only red in it: "failed/danger `#F85149`". decisions.md #6 mandates "one high-visibility color (red)" — it does not license a new hex. Judgement call rather than a hard break, and there is a real argument for a more saturated red over arbitrary video frames; but if that argument holds, the palette is the thing to amend. Expected: #F85149, or a documented palette addition. Actual: an undocumented one-off hex in a lib file.
- [ ] [Code review — Standards axis] Smell: Shotgun Surgery — the screenshot URL is spelled out in three separate files, and the code admits it. (1) packages/server/src/services/test-notes.ts ~40, noteScreenshotUrl: `/api/reviews/note/${noteId}/screenshot.png`; (2) packages/server/src/routes/reviews.ts ~225, the route literal `/note/:noteId/screenshot.png`; (3) apps/web/src/lib/reviews.ts ~52, uploadScreenshot's `/api/reviews/note/${...}/screenshot`. The doc comment on (1) says outright: "Must stay in step with the route that serves it (routes/reviews.ts); the HTTP round-trip test follows a stamped URL back through that route, so a drift shows up as a 404 rather than as a silently broken thumbnail." Changing the URL shape means editing three files in two packages. Compare attachmentRelPath in packages/core/src/paths.ts, which the lap got right — one exported builder both ends import. Expected: the same treatment for the screenshot URL, in core. Actual: three hand-written copies held in step by a test.
- [ ] [Code review — Standards axis] Smell: Duplicated Code — two seconds-to-clock formatters that disagree past an hour. packages/server/src/services/test-notes.ts ~263: `function clockTime(seconds)` → `${Math.floor(whole / 60)}:${String(whole % 60).padStart(2, '0')}`. apps/web/src/lib/format.ts ~66: `fmtClock(seconds)`, which handles hours. Same domain concept, two implementations, and they diverge: 3700 seconds renders as "61:40" in the promoted ticket's context paragraph but "1:01:40" in the player and the note thumbnail tooltip. Walkthroughs are short so an hour-long recording is unlikely, but the two are already out of step and nothing holds them together. Per CLAUDE.md's package map, @runcastle/core is the IO-free home both sides already import from (both files import from it). Expected: one formatter in core, used by both. Actual: two, silently divergent.
- [ ] [Code review — Standards axis] Smell: two different mechanisms answer one predicate ("does this note have a screenshot?") in the same file. packages/server/src/services/test-notes.ts ~265, screenshotParagraph: `if (!existsSync(annotationPath(note.id))) return undefined`. Same file ~409, screenshotSuffix: `return note.screenshotUrl ? ... : ''`. Both are asking whether the PNG is on disk — one by stat, one by reading the field rowToNote already stamped from that same stat. Divergent Change / Duplicated Code: a future change to how presence is decided (say, a size or signature check) has to find both. Expected: one of them calls the other, or both read the stamped field. Actual: two answers to one question, three hundred lines apart.
- [ ] [Code review — Standards axis] Smell: Duplicated Code — findNote duplicates findReviewTicket in the same file. packages/server/src/routes/reviews.ts ~180: findNote wraps getNote in the identical `try { ... } catch (e) { if (e instanceof NotFoundError) return undefined; throw e }`. findReviewTicket at ~61 in the same file is the same shape around getTicket (plus one `kind === 'review'` filter). The new function's own doc calls it "The note-keyed counterpart of {@link findReviewTicket}, and it exists for the same reason". Expected: one small "lookup or undefined" helper both call. Actual: the pattern copied. Low severity — flagging it because this file now has two and a third route would make three. Worth noting separately: the security property both exist to provide is correctly implemented — the screenshot path is computed via annotationPath(note.id) from the row the DB returned, never from the URL segment, so a traversal attempt in :noteId just fails the lookup and 404s.
- [ ] [Review process] The browser drive could not run this pass — no UI criteria were verified by driving. mcp__runcastle__review_drive({action:"start"}) refused with "Working tree has uncommitted changes — commit or stash first". The checkout is on main with two unrelated modified files: packages/server/src/services/git.ts (in-progress worktree-reclamation work — a new reclaimOrphanedWorktree function) and packages/server/test/project-session.test.ts. Neither belongs to this lap. I did not commit or stash them; that is the human's work to land. Consequence: acceptance criteria 2-5 (custom player controls scrub/pause/play; Annotate → draw → undo → save producing a note with a thumbnail; the PNG round-tripping over HTTP and living at ~/.runcastle/annotations/<noteId>.png; deleting an annotated note removing its PNG; promoting an annotated note yielding a ticket whose context names .runcastle-attachments/<noteId>.png) are UNVERIFIED at the UI. They are covered by unit and seam tests on the branch, but nobody has yet watched the loop work in a browser. To re-run the drive: commit or stash the two files above, then re-burn this review ticket.
- [ ] [Review summary — read me first] All three implementation tickets landed; this is a review of a complete feature, not a partial one.

CODE REVIEW ran on the full branch diff against main (merge-base daeee74, 12 commits, 25 files, +2811/-43), on two axes kept separate.

Spec axis — 2 findings, both noted. Worst: the burn-time screenshot never reaches the fix agent under the DEFAULT burn mode on Windows and macOS. burnWorkspace='auto' resolves to 'isolated' off-Linux, and isolated mode git-clones the workspace into /home/agent/repo — an untracked, git-excluded PNG cannot survive a clone, so the ticket context points the agent at a path that isn't there. Verified through resolveBurnWorkspaceMode, buildIsolatedSetupCommand and buildWorkspaceNotes. This is the headline problem of the lap: the plumbing works everywhere the tests look and fails on the machine this repo is developed on. Second: excludePath appends '.runcastle-attachments/' to the repo's .git/info/exclude and never removes it.

Standards axis — 5 findings, all noted. No hard CLAUDE.md violations: Bun/ESM/strict clean, no `any`, tRPC inputs zod-validated, migration 0027 drizzle-generated, and the mutating-service-emits-an-event rule holds (attachScreenshot emits note.screenshot and re-renders). Worst: Shotgun Surgery — the screenshot URL is hand-spelled in three files across two packages, with a comment admitting they must be kept in step; compare attachmentRelPath in core, which the same lap got right. Also: a hand-concatenated Windows path in the copy command (cites the CLAUDE.md node:path rule directly), an off-palette red, two divergent seconds-to-clock formatters, two mechanisms for one "has a screenshot?" predicate, and a duplicated NotFound-to-undefined helper.

The two things the ticket asked me to look hardest at both check out. Upload/serve security: the screenshot path is computed via annotationPath(note.id) from the DB row, never from the URL segment — a traversal attempt in :noteId just fails the lookup and 404s, and the POST additionally checks the PNG magic bytes so the GET's unconditional image/png is honest. test-notes.md compatibility: the '(screenshot: <abs path>)' suffix is safe, because the readers (the revisit skill and lap-kickoff briefings) are agents reading prose, not a parser, and the '## Lap N' sectioning is untouched.

VERIFIED BY RUNNING, not by driving. The browser drive was refused (see the separate note) — the human has uncommitted work in the tree, so acceptance criteria 2-5 are unverified at the UI. I ran the suites instead, in a throwaway git worktree in the system temp dir, which I have since removed; the checkout is exactly as I found it. Full vitest suite green: 98 server test files, 1286 passed / 9 skipped / 0 failed; 25 web+core files, 615 passed / 1 skipped; monorepo typecheck clean across core, server, web and design-system; web build succeeds. The seams the drive would have covered are covered by tests that do pass: the HTTP round-trip ('upload the frame, fetch it back as a PNG', 'serves it at exactly the URL the notes list stamps'), the delete-removes-the-PNG path, the promotion context (asserted verbatim, including the Read instruction and 'captured at 0:42'), and all six workspace-prep tests including 'leaves the workspace clean, so no commit of the agent's can pick it up'. What no test covers, and what nobody has therefore seen work: the player controls, the drawing/undo interaction, and the thumbnail rendering in a real browser.

Two process points. This ticket told me to run 'bun test in packages/server'; that is the wrong runner for this repo — packages/server has no test script and the suite is vitest from the root. `bun test` produced no output and had to be killed after ten minutes. Worth fixing in the review-ticket template. And one efficiency observation, not promoted to a finding: rowToNote now calls existsSync per row, so listByFeature costs one stat per note on every poll.

## Lap 2

- [ ] [Code review — Spec axis] The clone-side re-exclude that lap 2 added is real, correct, and written down nowhere. packages/server/src/workflows/ticket-burner.ts:590, inside buildIsolatedSetupCommand, the guarded branch does four things: `if [ -d "$attachmentsDir" ]; then mkdir -p "$cloneAttachmentsDir" && cp -r "$attachmentsDir/." "$cloneAttachmentsDir/" && mkdir -p "$ISOLATED_REPO_PATH/.git/info" && printf '%s\n' '.runcastle-attachments/' >> "$ISOLATED_REPO_PATH/.git/info/exclude"; fi`. decisions.md #9 licenses only the copy: "buildIsolatedSetupCommand copies .runcastle-attachments/ from the mounted workspace (/home/agent/workspace) into the container-native clone (/home/agent/repo) after the git clone, when the directory exists." spec.md "Riding into the burn" likewise says only "the container setup additionally copies .runcastle-attachments/ from the workspace into the clone after cloning". Neither mentions writing the clone's own .git/info/exclude. Expected: the docs describe what the setup command does. Actual: the second half is undocumented. Keep the code — it is the right answer to the review ticket's own question (the copied PNGs must not ride the clone's landing path into a commit), and packages/server/test/burn-attachments.test.ts:349 "leaves the clone clean, so nothing the agent commits can push them back" asserts `git status --porcelain` is empty in a real clone. This is a documentation gap, not a behaviour one: amend decision #9 (or spec.md) to name the clone-side exclude, so the next lap does not read it as stray scope.
- [ ] [Code review — Spec axis] Under overlapping burns in MOUNTED mode the attachments are not "unstageable" the way the code comment promises, and decisions.md #10 understates the cost. The retained comment at packages/server/src/workflows/ticket-burner.ts:2008-2014 claims: "the images are unstageable from the moment they exist — no commit of the agent's can pick them up, and sandcastle's end-of-run dirty check still sees a clean tree." decisions.md #10 accepts the race but bounds it: "The concurrent-burn race (one burn's cleanup un-excluding another's live directory) is accepted — the worst case is the attachments dir briefly visible in `git status`". Verified chain: excludePath/unexcludePath both resolve `info/exclude` against the COMMON git dir (packages/server/src/services/git.ts:718, excludeFilePath, via `git rev-parse --git-path info/exclude`), so all concurrent burns of one repo share exactly one line; excludePath:735 is a no-op when the line is already present, so burn B's call adds nothing of its own; unexcludePath:755 then removes that single shared line unconditionally when burn A finishes. From that moment burn B, still running, has NO protection — in mounted mode the exclude is its only one (clearAttachments only runs after B's own run() returns). An agent doing `git add -A` then stages and commits the PNGs, which is materially worse than "briefly visible in git status". Expected: the comment and decision to describe the actual worst case. Actual: both claim a protection that a concurrent burn can revoke. Correctly scoped, and this is why it is low severity: isolated mode — the burnWorkspace:'auto' default on Windows and macOS — is unaffected, because the clone carries its own .git/info/exclude (line 590) which unexcludePath never touches. Exposure is mounted-mode only, i.e. Linux hosts or an explicit burnWorkspace:'mounted'. Fix is wording, not mechanism, unless the race is worth coordinating.
- [ ] [Drive — verified by running] unexcludePath does not restore info/exclude byte-for-byte when the file had no trailing newline, contradicting its own documented contract. packages/server/src/services/git.ts:743-752, unexcludePath's doc: "Everything else is written back byte-for-byte, line endings included, so a file the human has their own entries in comes out unchanged." Reproduce (I ran exactly this against a real scratch git repo, importing the two functions directly): write `.git/info/exclude` as "node_modules/\n*.log" (no terminating newline — an editor that does not add one, or a hand-edited file), then call excludePath(repo, '.runcastle-attachments/') and unexcludePath(repo, '.runcastle-attachments/'). Expected: "node_modules/\n*.log", unchanged. Actual: "node_modules/\n*.log\n" — the human's last line gains a newline that was not there. Cause: excludePath:736 compensates for the missing terminator by writing `\n${pattern}\n`, and unexcludePath only ever removes whole lines, so the compensating newline it introduced stays behind. The other cases DO round-trip exactly — I checked CRLF ("node_modules/\r\n*.log\r\n" comes back identical, the added line's own \n being the only LF), a file with pre-existing human entries and a trailing newline, an empty file, and a second removal (correct no-op). Severity is genuinely low: git treats the two files identically and a trailing newline is the POSIX-correct form anyway. Flagging it because the function's whole reason to exist is leaving the human's file as it found it, packages/server/test/burn-attachments.test.ts:204 asserts "puts info/exclude back exactly as it found it when the run {returns,throws}" against fixtures that all end in a newline, so the suite cannot see this. Fix is one branch in excludePath (remember whether it added the leading newline) or one in unexcludePath, or simply narrowing the doc.
- [ ] [Code review — Standards axis] The new packages/core/src/routes.ts says it spells runcastle's non-tRPC paths once, but only the screenshot half moved — the bigger media route still drifts in exactly the way its own header describes. The module header (packages/core/src/routes.ts:1-15): "The HTTP paths runcastle's non-tRPC surface answers on, spelled once. These routes carry media — a walkthrough the player scrubs, a screenshot an `<img>` fetches ... Three parties have to agree on the same string: the Hono route that serves it, the service that stamps the URL onto a wire type, and the browser code that fetches or uploads to it." The walkthrough is named in that sentence and then left out of the module. Verified: packages/server/src/routes/reviews.ts:36 still hand-builds "`/api/reviews/ticket/${ticketId}/walkthrough.webm`" against a route registered separately at :162 as '/ticket/:ticketId/walkthrough.webm' — the same two-party literal the screenshot pair was just rescued from — and it re-spells the '/api/reviews' mount rather than using the REVIEWS_BASE the same lap introduced. apps/web/src/lib/reviews.ts:39 likewise hand-spells "`/api/reviews/${encodeURIComponent(featureId)}`". Baseline smell: Shotgun Surgery (changing the walkthrough URL shape still means editing two files), plus a doc that overclaims its own scope. Judgement call, not a hard violation — decisions.md #11 asked only for "the screenshot URL spelled once in @runcastle/core", and that part is genuinely complete: I confirmed all three former call sites now import from core (services/test-notes.ts:10 noteScreenshotUrl, routes/reviews.ts:4-5 NOTE_SCREENSHOT_ROUTE/NOTE_SCREENSHOT_UPLOAD_ROUTE, apps/web/src/lib/reviews.ts:1 noteScreenshotUploadUrl), and index.ts:2 mounts on REVIEWS_BASE. Expected: either the walkthrough route joins the module, or the header narrows to the screenshot routes it actually owns. Actual: a "spelled once" module that covers one of the two media routes it names.
- [ ] [Code review — Standards axis] Smell: Duplicated Code — the exclude PATTERN is now hand-spelled in four places, in a lap whose theme was spelling things once. `${ATTACHMENTS_DIR}/` — the directory name plus the trailing slash that makes it a git directory pattern — appears at packages/server/src/workflows/ticket-burner.ts:590 (inside the isolated setup shell command, as the literal `printf '%s\n' '.runcastle-attachments/'`), :653 (`const marker = `${ATTACHMENTS_DIR}/``, reading the path back out of a ticket context), :730 (`await unexcludePath(repoPath, `${ATTACHMENTS_DIR}/`)`), and :2016 (`await excludePath(project.repoPath, `${ATTACHMENTS_DIR}/`)`). ATTACHMENTS_DIR itself is properly shared from core, so the directory name is safe; it is the pattern — the "+ '/'" that makes the exclude match — that is re-derived each time, and :590 additionally re-implements what excludePath does, in shell, because that write happens inside the container. Expected, by the same logic decisions.md #11 applied to the screenshot URL ("the screenshot URL spelled once in @runcastle/core") and that paths.ts already applies to attachmentRelPath: one exported ATTACHMENTS_EXCLUDE constant beside ATTACHMENTS_DIR, imported by all four. Actual: four spellings, one of them in a shell string where a typo cannot be typechecked. Judgement call and low severity — they agree today, and the add/remove pair is symmetric, which is what matters. Worth doing because :590 and :730 must match exactly for the clone-side and host-side excludes to mean the same thing, and nothing in the type system holds them together.
- [ ] [Code review — Standards axis] Smell: Middle Man — the lookup-or-undefined extraction left findNote as a pure pass-through. packages/server/src/routes/reviews.ts:199-201, in full: `function findNote(ctx: AppCtx, noteId: string): TestNote | undefined { return lookupOrUndefined(() => getNote(ctx, noteId)) }`. Lap 1's version earned its keep by carrying the try/catch; now that lookupOrUndefined:67-73 holds it, findNote adds nothing — no filter, no transformation, no extra guard. Its sibling findReviewTicket:85-86 is the counter-example and shows what a wrapper should look like here: it calls the same helper and then adds `return ticket?.kind === 'review' ? ticket : undefined`. Expected: inline it — the two call sites (:221 and the GET at :239) read as well or better with `lookupOrUndefined(() => getNote(ctx, c.req.param('noteId')))`. Actual: a named function that only forwards. Judgement call and the lowest-severity item I have — the extraction itself was the right move and decisions.md #11's "a shared lookup-or-undefined helper replacing the duplicated NotFound-catch in routes/reviews.ts" is genuinely delivered. This is just the leftover shell. Worth recording separately: the security property findNote exists to provide still holds, and I checked it live rather than by reading — the screenshot path is computed via annotationPath(note.id) from the row the DB returned, never from the URL segment, and against the running drive server `GET /api/reviews/note/..%2F..%2Fconfig.json/screenshot.png` returns 404, as does an unknown note id, while a real one returns 200 with Content-Type: image/png.
- [ ] [Code review — Standards axis] Dead import left behind by the node:path cleanup. packages/server/src/workflows/ticket-burner.ts:2 imports `basename` — `import { basename, dirname, join, posix as posixPath, win32 as winPath } from 'node:path'` — but after ticket 6's fix the only remaining use is the platform-scoped `path.basename(src)` at :705, inside buildAttachmentCopyCommands where `path` is `winPath` or `posixPath`. Grepping the file for "basename" returns exactly two hits: the import at :2 and `path.basename` at :705. The top-level binding is unreferenced. Expected: drop it from the import list. Actual: it survives. Trivial, and I checked that no tooling catches it before reporting: `bun run typecheck` passes clean (exit 0) across core, server, web and design-system, and the repo has no lint script at all (root package.json scripts are dev, dev:tool, typecheck, test, test:watch, deploy:worker, release, postinstall), so nothing flags unused imports. Flagging it only because removing the hand-concatenated `\\` in favour of node:path was itself one of the five tidiness items decisions.md #11 bundled — the fix landed correctly (`const dest = path.join(dir, path.basename(src))` at :705, with the platform-correct path module, honouring CLAUDE.md § Conventions "Windows paths: always node:path (join, resolve); never hand-concatenate"), and this is the one loose thread it left.
- [ ] [Review process] The review drive starts on an EMPTY per-branch database, so a review of THIS feature cannot be driven without hand-seeding it first. What I did: mcp__runcastle__review_drive({action:"start"}) succeeded this lap (the tree was clean — thank you), switched the checkout to feature/video-annotation-for-reviews and had a dev server up at http://localhost:22192 by the time I first polled status. What I got was the first-run "WELCOME TO RUNCASTLE / Set up runcastle →" screen: driveEnv points RUNCASTLE_DATA_DIR at ~/.runcastle-drive-video_annotation_for_reviews/, whose runcastle.db had been freshly migrated and held no projects, no features, no tickets and no reviews. Nothing to annotate: this feature's whole surface is a walkthrough video inside a review body, and a virgin database has neither. To drive it at all I had to seed that throwaway DB by hand — ATTACH the real ~/.runcastle/runcastle.db and copy the projects rows, the feat_DFF9lgSEFhBh feature and its 8 tickets, then copy a real walkthrough.webm from ~/.runcastle/reviews/tkt_Wh0x7SHwp1ym/ into ~/.runcastle-drive-video_annotation_for_reviews/reviews/tkt_cw4d5vbDA0eV/, and set the feature's phase to 'review' so the review body would render. Only then was there a player to test. Expected, arguably: a drive of a review-phase feature lands on something reviewable — a seeded fixture, or the drive DB seeded from the real one for the feature under review. Actual: an empty app, and a reviewer who has to reconstruct the scenario before the first click. Not a defect in THIS lap's code and not blocking — everything after the seeding worked, and the schema comparison was itself informative (the drive DB had test_notes.video_timestamp, confirming migration 0027 applies cleanly to a fresh database while the main-branch DB lacks it). Recording it because it is the reason a UI-only feature went two laps unwitnessed, and because the next reviewer of any review-surface feature will hit exactly this.
- [ ] [Review summary — read me first] Lap 2 works. All three implementation tickets landed, and this is the lap where the loop was finally watched end to end in a browser — the thing lap 1 could not do.

THE DRIVE RAN. Every UI criterion is now verified by driving, not by inference. Custom controls: play advances the video, the button flips to "pause" and back, the scrub bar spans 0→909.5s at 0.05 step and a seek to 42 lands on exactly 42, and the readout shows "0:42 / 15:09" from the shared core formatter. Annotation: Annotate opens the overlay with Undo/Clear/Save correctly disabled, drawing enables them, a second stroke then Undo removes exactly that stroke (6213 → 3479 opaque pixels), and Save is gated on note text as designed. The pen is right — I sampled the canvas and every opaque pixel is rgb(248,81,73) = #F85149, the palette danger red, so decision #11's colour change is confirmed at the pixel and not just in the source. Save produced note_ltYqbTQHjwjw with videoTimestamp 42.0 and a 302KB PNG at ~/.runcastle-drive-.../annotations/note_ltYqbTQHjwjw.png; the thumbnail appeared in the notes list with NO refresh, at full 1600x1000, with the alt text "the annotated frame this note is about". I opened the baked PNG: it is the paused frame at 0:42 with the red stroke composited on top, one stroke, the undo honoured. The HTTP round-trip is byte-identical to the file on disk, Content-Type: image/png, 404 on an unknown note and on a traversal attempt. Jump-to-moment: from 202s PLAYING, clicking the note's "0:42" seeks to exactly 42 and pauses — and while annotating at 300s the same click leaves the frame at 300, so the guard holds. Delete removed the note row, its PNG, and made the URL 404 while leaving the other note's PNG alone. Promotion minted a ticket whose context reads "An annotated screenshot of the problem is at .runcastle-attachments/note_ltYqbTQHjwjw.png in your workspace, captured at 0:42 of the review walkthrough — Read it before starting" — relative path, Read instruction and clock-formatted timestamp, all three. And the rendered test-notes.md gained the "(screenshot: <absolute path>)" suffix exactly as specced.

CODE REVIEW ran on the lap-2 diff (5539764..HEAD, 10 commits, 22 files, +737/-148), two axes kept separate.

Spec axis — 2 findings, both noted, both documentation rather than behaviour. Worst: the retained comment in ticket-burner.ts and decisions.md #10 both promise the attachments are "unstageable" / worst-case "briefly visible in git status", but because excludePath/unexcludePath share ONE line in the common git dir, a concurrent burn's cleanup strips the other burn's only protection mid-run, and in mounted mode an agent's `git add -A` can then commit the PNGs. Scoped small: isolated mode — the Windows/macOS default — is immune, because the clone carries its own exclude. Second: that clone-side exclude is correct and undocumented. Both lap-1 spec findings are genuinely FIXED: the isolated copy is guarded, copies contents not the directory, and excludes in the clone; the exclude is unwound on both the return and the throw path (ticket-burner.ts:2461 and :2464).

Standards axis — 4 findings, all noted, none a hard CLAUDE.md violation. Worst: the new core/routes.ts declares it spells the non-tRPC paths "once" and names the walkthrough in that sentence, but only the screenshot routes moved — the walkthrough URL is still hand-spelled in two places and the web client re-spells the mount. The screenshot cleanup itself is complete at all three call sites; I checked each import. Also: the exclude pattern re-derived in four places, findNote reduced to a pure pass-through, and a dead `basename` import. All five of decisions.md #11's cleanups landed.

WHAT I DID NOT DO: no live burn. The default mode here is isolated and exercising it would spawn a container and a second agent on your machine while this review is itself a burn, which I judged not safe to do unattended. So criterion 5 is verified at the seams instead, as the ticket allows — by reading buildIsolatedSetupCommand and the cleanup wiring, by running excludePath/unexcludePath against a real scratch git repo (which is how I found the trailing-newline note), and by the suite's isolated-mode tests, which do a real clone and assert `git status --porcelain` is empty in it. The agent-side path resolving inside a real container remains unwitnessed.

GREEN: full vitest suite from the repo ROOT — 126 files, 1926 passed, 13 skipped, 0 failed; `bun run typecheck` clean; web build succeeds. The runner correction in this ticket was right and saved the ten minutes it cost last lap.
