# Improve map workflow UI/UX + Make markdown render correctly

Currently the map workflow (when the feature is too big), is a little confusing. The map shows at the top, as raw markdown (I think this is an issue with all docs throughout the tool not just the map). The user has to go down to see the waypoints (which are the important part). They're stacked on top of each other. So the user has to go even more down to see the terminal which is very annoying. Once a waypoint is done (or the initial grill), the user has to explicitly click "close session" then click on the next waypoint. I feel like we can streamline this a lot better

- Slug: improve-map-workflow-ui-ux-make-markdown-render-correctly
- Created: 2026-07-26T07:25:00.188Z
