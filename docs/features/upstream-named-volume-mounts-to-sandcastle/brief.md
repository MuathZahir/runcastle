Deferred from persistent-burn-cache-volume (decision 3): that feature ships a bun `patchedDependencies` patch of sandcastle 0.12.0 so a mount can name a Docker/Podman volume (`resolveUserMounts` currently rejects any hostPath that is not an existing host directory; the emitted `-v` flag already accepts `name:/path`). This feature is the follow-up: open the PR upstream, land it, bump the pin, delete `patches/@ai-hero/sandcastle@*.patch`. It must not swallow any cache/slot behaviour — only the mount-shape change moves upstream; runcastle's volume, slot and probe logic stay where they are.
