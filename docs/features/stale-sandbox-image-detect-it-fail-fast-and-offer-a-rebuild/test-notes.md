# Test notes

## Lap 1

- [ ] Standards axis — packages/server/src/services/setup.ts, new refreshSandcastleConfig()/prepareSandboxBuildContext(): CLAUDE.md “Conventions (SPEC §12)” requires “Every service function that mutates emits an event.” I inspected the branch hunk: the exported service helper mutates the app-owned scaffold with mkdirSync/copyFileSync, but the refresh path emits no event. Expected the mutation to publish an event so affected observers can invalidate consistently with the documented service contract.
- [ ] Standards axis — judgement-call Duplicated Code smell in packages/server/src/workflows/ticket-burner.ts. The branch introduces identical RUNTIME_BINARY and AGENT_BINARY maps (`claude-code → claude`, `codex → codex`) and duplicates the full “<binary> is not installed in image <image> … Rebuild image” message in missingImageRuntimeMessage() and missingAgentBinaryMessage(). Expected one shared mapping/message formatter so runtime additions and operator guidance cannot drift between the precheck and catch paths.
- [ ] Spec axis — packages/server/src/workflows/ticket-burner.ts partially implements acceptance criterion 2: “bin = codex or claude per the resolved model's runtime” and “Cache the result per (image, runtime).” I inspected burnRun/resolveBurnDeps: the precheck probes only deps.runtime from the run-default model, while ticketCredentials()/resolveTicketModel() may resolve individual tickets to the other runtime. A mixed-runtime burn can therefore probe Codex, then create a Claude ticket container before discovering Claude is absent; the cache key is likewise fixed to the default runtime. Expected every distinct burnable ticket runtime to be probed once before ticket containers are scheduled. Existing coverage only proves two same-runtime tickets share one probe.
- [ ] Drive finding — the AFK burns UI walkthrough could not start. review_drive refused the start request with “Working tree has uncommitted changes — commit or stash first.” Expected the feature checkout/dev server to start so the missing, stale, and healthy image-row states and their Build/Rebuild flows could be exercised. I did not retry, stash, or modify the human’s checkout; no browser, recorder, server, or drive slot was started.
- [ ] Review summary — Standards axis: 2 findings. Worst within Standards is the hard CLAUDE.md event-contract violation in the new mutating scaffold-refresh service; the other is duplicated runtime mapping/operator-message code. Spec axis: 1 finding. Worst within Spec is the mixed-runtime precheck gap: only the run-default CLI is probed, so a ticket assigned to the other runtime can still create a container before failing, contrary to the per-resolved-runtime/cache criterion. The remaining classification, doctor stale-status, image-row state handling, and scaffold-refresh requirements are represented in the diff and focused tests. Drive verification: none of the UI criteria could be exercised because the drive start was refused for an uncommitted working tree; no browser/recording/server started, and the refusal was not retried.
