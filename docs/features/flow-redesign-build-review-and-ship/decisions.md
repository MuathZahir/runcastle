# Decisions — Flow redesign: build, review, and ship

## 1. The walkthrough video and annotation loop is the priority of this flow; it must work perfectly
**Decision:** Of the seven areas this flow covers, the walkthrough player and the annotate → note → fix-ticket → next-lap loop come first. The walk gives it the most attention (every scrub, pause, draw, save, jump and dead end), its grilling waypoint is the deepest, and the redesign is judged first on whether that loop works end to end without friction. Other areas are redesigned too, but none may trade against this one.
**Why:** The human's standing complaint: "the annotation flow doesn't work well and isn't very user friendly", restated at charting as "I want to get it to work perfectly". The loop is also the one the human chose to keep whole rather than split, because it cannot be judged in halves.

## 2. Ideation is mapped
**Decision:** This flow is charted into a waypoint map (ADR-0001) rather than grilled in one window: an AFK research pass over the prior designs and audit findings, three serial walk tasks that write `flow-map.md`, a confirm-the-map grilling with the human, then one grilling per design area and one for code shape / `styles.css` retirement. Spec and tickets happen in the converge session, which is also where the lap-1 slicing question is asked.
**Why:** The walk alone (success / failure / cancel burns, a review with a walkthrough, drives with and without a dev command, annotation, both triage paths, a lap 2, a real conflict, merge, shipped) is hours of agent time and dozens of screenshots — more than one context window can hold and still spec from — and seven design areas plus six prior designs to reconcile sit on top of it. Confirmed by the human on 2026-09-03.

## 3. The as-is map of review, test drive, annotation, triage and laps is `flow-map.md` §B–§F; the annotation complaint has a root cause
**Decision:** `flow-map.md` sections B (review landing), C (test drive), D (walkthrough player + annotation), E (notes triage) and F (laps) are the recorded baseline for those areas, walked live on 2026-09-04 with screenshots `v01`–`v33` under `prototypes/walk-fixtures/shots/`. The redesign treats these as facts, not hypotheses: (1) **the annotation loop breaks, it is not merely awkward** — the second stroke on a frame (or the first stroke of a second annotation) crashes the whole feature view to the error boundary, deterministically, because `startStroke` reads the pointer event's `currentTarget` inside a lazily-run React state updater; the human can save at most one single-stroke annotation per page load, and a circle-plus-arrow is impossible. (2) The player has no loading state and no error state — a 20-minute recording and a corrupt file both look like a dead card for as long as they take — and no keyboard, frame-accuracy (2 s per slider pixel at 20 min) or a way to keep the whole frame and its controls on screen at once (the sticky bar covers the frame's top). (3) Jump-to-moment moves the playhead but never brings the player into view; editing or triaging an annotated note hides its picture and timestamp. (4) F3 and F22 are closed as the research said; batch-promote appends no review ticket; a failed Iterate rolls back correctly but leaves no trace on the page. (5) From lap 2 the review-agent row and the walkthrough card vouch for lap 1's review beside a correctly lap-scoped account block; Burn counts and burns every pending ticket across laps with no lap grouping in the run view; the lap banner's past-tense copy renders before the lap session has run. The scratch feature is left in lap 2 with seven open notes for walk D.
**Why:** The human's verdict ("doesn't work well and isn't very user friendly") needed a location, and the walk found one that outranks the ergonomics list from the research: a crash on the second stroke explains a loop that feels broken far better than a thin pen or a low-on-the-page card. Recording the crash and the lap/vouching contradictions as walked facts — with the mechanism named — lets the confirm session put them to the human as baseline rather than re-deriving them, and lets the grilling waypoints (7, 8) start from "the loop must survive two strokes, show its evidence at triage and stop vouching across laps" instead of from the ergonomics alone.

## 4. The as-is map of conflict, merge and the shipped view is `flow-map.md` §G; F21/F8 are closed at the bar but the shipped record keeps living
**Decision:** `flow-map.md` section G is the recorded baseline for the conflict card, the merge dialog, the shipped body, the outcome doc, the read-only review view and the "Ask a question" Q&A terminal — walked live on 2026-09-04 by colliding the scratch repo's `main` with the feature branch, resolving through `use-resolve-conflict`, merging and shipping, with screenshots `m01`–`m25` under `prototypes/walk-fixtures/shots/`. The `DriveFailureCard` and `StopReviewDrive` branches walk C/D never reached are covered here too (§G). The redesign treats these as facts: (1) **F21 is closed** — Merge & ship opens a read-to-confirm `MergeFeatureDialog` (a green "what lands" summary + a warnings box), not a one-click merge. (2) **F8 is closed at the bar but not in the dialog** — over a standing conflict the bar's primary flips to "Resolve the merge conflict" and Merge & ship demotes to a secondary "Retry Merge & ship" with nothing disabled (decisions 2b/3), but that retry opens the *same all-green dialog that never mentions the conflict*, so a human reading only the dialog sees an all-green "what lands" over a branch that will re-conflict. (3) **The next-step bar visually collapses to one-word-per-line whenever the ConflictCard is up** — a real layout break in the state that most needs reading. (4) **The shipped record keeps living**: the lap banner still narrates lap 2 on a shipped feature; the outcome doc is written and dated (`Shipped:`) at the *pre-merge* commit and reprints the burner-digest noise verbatim; the conflict card's timestamp and the shipped hero both read `now ago`. (5) **The `ux-issues` "conflict card live in the read-only shipped view" item is confirmed**: the card at `ReviewBody.tsx:169` is gated on `conflict &&`, never on `readonly`, and its "Resolve with agent" is a live action; the read-only Test-drive card likewise gives a live "start it from the next step" instruction. (6) **Every session this surface spawns — resolve-conflict, drive-fix, Ask-a-question — boots onto Claude Code's "Yes, I trust this folder" prompt**, the same first-act friction walk D found for the lap session; and an ended Q&A conversation that captured no transcript vanishes on reload, leaving no record of what was asked. The scratch project is noted, not torn down: `greetings-pages` is left shipped as the record for the confirm/grill sessions.
**Why:** The confirm session (waypoint 5) and the merge/shipped grill (waypoint 9) need the conflict/merge/ship half as walked fact, not audit hypothesis — F8 and F21 were open findings and `ux-issues` listed the read-only conflict card as "left undone", and the walk settles all three against the live code: F21 and F8 are handled well at the *bar* level, so the redesign's work there is narrower than the findings imply (the dialog's conflict-blindness and the bar's layout break), while the shipped-record problems (a lap banner on shipped, an outcome doc dated before it shipped, a live conflict action in a history view, and a trust-prompt on every Q&A) are the surface's real open work. Naming them here — with the exact gate (`conflict &&` vs `readonly`) and the exact copy bug (`relTime` "now" + " ago") — lets waypoint 9 start from the fixes rather than re-deriving the map, and closes the three-walk baseline (decisions 3 and 4 together cover §A–§G).

## 5. The flow map is confirmed as the baseline; the human's own issues are added to it
**Decision:** `flow-map.md` §A–§G (walked 2026-09-03/04, shots `r01`–`r10`, `v01`–`v33`, `m01`–`m25`) plus `research/prior-designs.md`'s "still open" list are the confirmed baseline for this redesign, in the shape of onboarding's decision 1: the redesign is judged against these facts, and every listed dead end either gets a design decision or an explicit park. The human confirmed the map on 2026-09-04 and confirmed hitting the second-stroke crash personally — the crash is the located root of the standing complaint, with the ergonomics list behind it. The human added seven issues the walk could not have known, now part of the baseline and binding on the grill waypoints (6–9):
1. **The review page is cluttered and hard to navigate.** The important things — the walkthrough video and "Open app" — must be at the top and prominent; today the video is sixth on the page and Open app is below the fold. *(→ waypoint 7, layout; waypoint 8, player placement.)*
2. **The video must be bigger** — it should take up much more of the review page than the current 788 px card.
3. **The player needs playback-speed controls, defaulting to 1.5×** — AI browser-use walkthroughs are slow to watch at 1×. *(→ waypoint 8.)*
4. **There is far too much text on the review page** — the digest walls (burner digests, review prose, explainer paragraphs) are text most users won't read, and it reads as confusing rather than informative. Prose must be demoted to progressive disclosure; the page leads with evidence and state, not paragraphs. *(→ waypoints 6, 7, 9 — the outcome doc reprints the same walls.)*
5. **Test notes are not useful enough when the human tests the app themselves.** A self-testing human wants to paste screenshots into a note; today only a video annotation can carry a picture. Notes must accept pasted/attached images. *(→ waypoint 8; the upload route keyed by note id already exists.)*
6. **"Open app" should open in an integrated panel with annotation tools like the video's** — the human selects an area of the live app that needs fixing and a screenshot is attached to the note automatically. *(→ waypoint 7 with waypoint 8's tools; see decision 8 for the scope call this needs — `test-drive-improvements` locked "no embedded browser", which this revisits.)*
7. **The review evidence shown as current is stale by design.** The review agent reviews the code and records the video, then mints fix tickets; those tickets burn and change the app, but no re-review follows — so the video, digest text, defects and notes the page presents as the feature's current state describe a build that no longer exists. The notes/defects surfaces compound this and read as "confusing and outdated". Every piece of review evidence must be presented with its moment (what it reviewed, what has landed since), and the grills must decide how the page distinguishes fresh evidence from superseded evidence. *(→ waypoints 6, 7, 8; whether a re-review pass runs after fix waves is a workflow question — see the scope decision to come.)*
**Why:** The confirm session exists so the human can add what the walk missed, and they did: items 1–4 sharpen the "most crowded surface" question into a concrete ordering (video and Open app first, text last), items 5–6 extend the annotation loop beyond the video onto the live app and self-taken screenshots, and item 7 names a truth problem — the page vouches with stale evidence — that underlies the walk's cross-lap vouching finding but is broader than it. Recording them here, routed to their waypoints, is what carries the human's words to the other waypoint agents.

## 6. The visual bar and spacing rhythm; what a returning human sees first on the review page
**Decision:** This flow is judged by the two prior flows' standards, adopted verbatim: onboarding decision 1's visual bar (deliberate vertical rhythm, clear type hierarchy kicker → heading → one-line lead → control, readable secondary text, no cards that are mostly empty space) and project-chat decision 9's spacing rhythm (Tailwind 4 px scale: 8 px inside a control group, 16 px between fields, 24 px between sections/cards, 32 px header-to-body; nothing closer than 8 px or farther than 32 px except at a section boundary). On top of that, the review page — the most crowded surface in the app — gets an ordering rule: **evidence first, prose last.** A returning human sees, in order: the walkthrough video (big — much more of the page than today's 788 px card) and the Open app affordance at the top and prominent; then current state (checks, open defects/notes, lap position); prose (digests, explainers, review text) is demoted to progressive disclosure at the bottom or behind it. The digest wall never again opens the page.
**Why:** Confirmed by the human 2026-09-04: "I want the user to have the important things (video, open app, etc.) at the top and more prominent … there's also a lot of text in the review page which most users won't read, it's kind of confusing." The rhythm is a rule, not taste, so seven areas designed across several grill sessions land looking like one flow.

## 7. Self-testing gets the video's annotate-and-attach loop on the live app; pasted screenshots are the floor
**Decision:** Two locked intents. (a) **Notes accept pasted/attached images** unconditionally — a self-testing human pastes a screenshot into a note and it rides the existing note-screenshot pipeline (upload route keyed by note id, thumbnail, promotion attachment) exactly as a video annotation's PNG does. (b) **"Open app" opens in an integrated panel on the review page with the annotation tools the video has** — select an area of the live app, a screenshot with the selection is attached to a note. This deliberately reverses `test-drive-improvements`' "no embedded browser": that decision predates the human's self-testing experience and is superseded for the review surface. Because the dev server is cross-origin (an iframe displays but its pixels cannot be read client-side), a prototype waypoint (charted from this session) must prove the capture mechanism — proxying the dev server through runcastle's origin vs. a server-side capture — before waypoint 7 commits to a mechanism; if the prototype has not resolved when waypoint 7 runs, it designs the panel UI mechanism-agnostic and leaves the capture path to the prototype's verdict. Pasted screenshots (a) are the floor that works regardless.
**Why:** The human: "The test notes don't feel very useful … he would want to paste screenshots for example. I think it would make sense that 'Open App' would open in an integrated panel with tools like those for video annotation." The annotation loop is this flow's priority (decision 1), and extending its evidence-capture to the human's own testing is the same loop, not a new one.

## 8. Re-review is in scope: after fix waves land, the review evidence refreshes, and the UI always shows the latest
**Decision:** The staleness problem is fixed at both levels, here. (a) **Workflow: a re-review pass runs after a review's fix tickets land**, so the video, digest, defect list and checks describe the build as it now is — the brief's "reflect, don't change the workflows" fence is lifted for this one change by the human's explicit instruction. (b) **The evidence system keys on latest:** the video shown is always the latest recording, the defect/finding surfaces reflect the latest review pass, and every piece of evidence is stamped with its moment (what it reviewed, what has landed since); superseded evidence is visually demoted or archived, never presented as current. This subsumes the cross-lap vouching fix (research still-open 1: lap-scoped `reviewOutcome`/`reviewChecks`/`reviewWalkthroughUrl`) — the rule is one generation stronger: not just "this lap's review", but "the latest review of the current build". The design of the pass (trigger, what re-records, cost/opt-out, interplay with laps and with `review-findings-are-fixed-in-run`'s in-run fixing) is grilled in a dedicated waypoint charted from this session, after the review-landing and annotation grills.
**Why:** The human, on being offered the park: "I want the re-review to be done here. Make sure that the video, defect, etc. system works with this (the latest video should be the one shown, etc.)." The presentation fix alone would label evidence honestly but leave it stale; the human wants it fresh.

## 9. Scope: what of the research's "still open" list is in, what is parked
**Decision:** **In scope:** correctness items 1–5 (cross-lap review pick — now subsumed by decision 8; the orphaned fix ticket that vanishes its defect; the never-retired conflict state on shipped features; batch-promote appending the review ticket `ux-issues` dec. 9 requires — taken literally, every lap's batch gets one; `readonly` honoured by all ReviewBody cards); the entire annotation-ergonomics list 7–16 (the priority area, now including the second-stroke crash); and every dead end in `flow-map.md` §A–§G — each gets a design decision or an explicit park in its grill. Tidiness items that live in this flow's files (17–20, 22–28: hand-spelled URLs, dead code, unstable findings order, phase-gated polling, per-lane digests, lap grouping of run lanes, SPEC §15.6 amendments, the live-drive explainer) ride waypoint 10's code-shape decisions and this flow's tickets. **Parked:** the burner-docs wording items (6: mounted-mode `info/exclude` overstatement; 21: undocumented clone-side re-exclude) — docs amendments to the burner, not this surface; orphan-PNG cleanup and conflict-resolver attachments (stay parked as before); the empty review-drive database (research §1.4.12 — drive tooling, not this flow); and the Windows long-path/sandcastle mount bug (walk A dead end 1) — the *UI* must surface a readable error for a launch that dies in one second (waypoint 6's grill), but the sandcastle path-rewrite fix itself is its own fix outside this flow.
**Why:** The correctness items are on this flow's path and the brief calls server bugs there fair game; the ergonomics list is the stated priority; the tidiness items are in files this flow rewrites anyway, so skipping them would preserve debt inside freshly-touched code. The parked items are either other subsystems' docs/tooling or infra whose fix would swallow the flow.

## 10. Run view: lanes are the spine; the transcript is per-lane disclosure; lanes get a real visual language
**Decision:** RunBody is rebuilt lanes-first. A returning human sees, top to bottom: a run header (progress, elapsed, the run-level control), then the ticket lanes as the page's spine. The shared Agent/Events tab pane pinned to one ticket is gone — each lane expands in place to show its own agent transcript and events, and while the container boots the expanded lane shows the Events-tab boot narrative (container create, cache sync) instead of "waiting for the agent's first output…" (kills walk dead end 5). The lanes themselves are designed, not listed: each lane carries a **runtime icon + model badge** (Claude vs Codex — runtime is a declared property of the model in the config roster, and tickets can carry per-ticket model overrides, so lanes can genuinely differ within a run), status-driven color and motion on the foundation's tokens — a live animation while `burning` (not a static chip), an animated settle into done/failed, elapsed timer while burning, short-sha chip when done. Motion is subtle and state-driven, within decision 6's visual bar and spacing rhythm.
**Why:** The walk showed the transcript is the least trustworthy element on the page (blank for the first 15–20 s of every ticket, raw tool spam, a confident `<promise>COMPLETE</promise>` over a failed lane) while the lanes carry the actual state and controls — so decision 6's "evidence and state first, prose last" puts lanes on top. The human added the visual ask directly: "improve how the lanes look… nicer visuals and animations, maybe icons for what type of model is running (claude or codex)."

## 11. A failed run gets two honest exits: per-lane Waive, and "Continue to review" once every lane is terminal
**Decision:** (a) A failed lane offers **Waive** beside Retry / Retry fresh: it cancels the ticket through the existing cancel machinery, the lane leaves the red failure state and reads as deliberately set aside, and the waived ticket is carried into review as explicitly-unfinished work (surfaced in the review landing's account — waypoint 7 designs where; the verb and its lane state are decided here). (b) When every lane is terminal and at least one ticket is done, the next-step bar offers **"Continue to review"** as a secondary beside the primary "Resume burn" — the honest partial-completion exit matching what gate G4 already believes ("every ticket reached a terminal state"). Continuing hides nothing: failed and waived tickets remain visible open work at review.
**Why:** Walk dead end 2 — a permanently-failing ticket loops forever with no exit but retry, while the G4 panel says "Ready to advance" and the bar says "Resume the burn"; the only escape is the gate's generic "Override with reason…", which buries the common "this one ticket is hopeless, the rest is fine" case behind a scary hatch. Confirmed by the human 2026-09-04.

## 12. Cancel run confirms; a deliberate stop is `stopped`, not `failed`; Stop ticket stays one click
**Decision:** (a) **Cancel run gets a confirm dialog** on the foundation's dialog primitive (never native `confirm()`), stating the blast radius: it stops N burning agents, finished work is kept, the burn can resume later. Retry fresh's native `confirm()` moves to the same primitive. (b) **`stopped` becomes a visually distinct lane state** — neutral/amber, never the red failure chip — covering both "stopped by user" and "orphaned — the run ended (cancelled)"; red is reserved for real failures. Run summaries speak the same language ("1 done · 1 failed · 2 stopped", never counting stops as failures). Stopped lanes keep Retry / Retry fresh. The state can derive from the already-recorded cause strings; no schema change is required unless implementation finds the strings unreliable. (c) **Stop ticket stays one click** — it is scoped to one agent and is the control reached for when an agent is visibly going wrong; friction there costs the most at the moment speed matters.
**Why:** Walk dead ends 3 and 4: cancel-run (kills every agent) was one unconfirmed click while the strictly less destructive Retry fresh confirmed, and a deliberate human stop was indistinguishable at a glance from a crash. Confirmed by the human 2026-09-04, including the Stop-ticket exception.

## 13. Transcript hygiene: protocol markers become UI, sandbox paths become repo-relative, failure verdicts explain themselves; Events demotes to a collapsed run timeline
**Decision:** All renderer-level, no workflow changes. (a) **Protocol tokens are swallowed and rendered as UI** — `<promise>COMPLETE</promise>` becomes a small "agent reported complete" marker at the transcript's end, and the same treatment covers any other protocol tokens the burner prompt defines. (b) **Sandbox-internal paths are rewritten repo-relative** in tool lines (`/home/agent/cache/slots/1/repo/src/App.tsx` → `src/App.tsx`). (c) **A failed lane's expanded view opens with a structured verdict strip** above the transcript: what the burner checked and what it found (e.g. "The burner verifies commits landed on the ticket branch — none did"), so a transcript ending in confident completion is framed by the verdict rather than contradicting the lane unexplained; the lane's collapsed summary keeps the short form. (d) **The Events tab demotes to a collapsed "Run timeline" section under the lanes** (run start, docs digest, per-ticket landmarks, run summary) — the debugging record, not a co-equal surface; per-ticket event detail lives in each lane's expansion per decision 10.
**Why:** Walk dead ends 6 and 7 — the burner's wire protocol leaked as the agent's last words, tool lines spoke in container paths meaningless to the human, and `failed: agent made no commits` sat under a happy transcript with nothing connecting the two accounts. Confirmed by the human 2026-09-04.

## 14. The run view narrates the review agent's in-run fixing; the header counts honestly; lanes group by lap
**Decision:** All presentation of events the workflow already emits — no workflow change. (a) **The review ticket's lane is visibly a different kind** — a "review" badge/icon beside decision 10's runtime icon, because its output is findings, not commits. (b) **Fix tickets minted mid-run arrive as a visually grouped "review fixes" wave** under the review lane, each badged with the defect it fixes and linked to the finding, arriving with decision 10's motion so the human sees the run grow instead of discovering unlabelled extra lanes. (c) **The run header recounts honestly**: "Burning 3 tickets · +2 fixes from review — 2 done", always stating done/failed/stopped; during a solo per-ticket retry it speaks about that retry ("Retrying #4"), never whole-lap numbers (closes walk dead end 8). (d) **Lanes group by lap with a lap divider** when the ledger spans laps (Burn burns every pending ticket across laps), matching the ledger's `LapSections`, so a lap-2 burn never mixes lap-1 leftovers in unlabelled.
**Why:** `review-findings-are-fixed-in-run` folds minted fix tickets into the same run, but the walk found the lanes just appear among the others, the bar's total silently mutates, and nothing marks the review lane as a different kind of work; the run view is where in-run fixing happens, so it must narrate it. (Rendering a defect as "being fixed" on the review page is waypoint 7's.) Confirmed by the human 2026-09-04.

## 15. Success settles before it advances; runs become navigable history
**Decision:** (a) **The auto-advance to review stays — no click added** — but success gets a landing: the lanes play decision 10's done-settle, a brief all-green beat, then the transition to review; a run watched to success is seen to succeed before the page changes. (b) **The status bar's "N runs" counter becomes real navigation** to a read-only run record: the same lanes-as-spine layout in terminal form, with the run timeline and per-ticket digests, and each lane's transcript when still in server memory (today's honest "transcripts are held in server memory for the current burn" note when it is not). No new persistence — this renders what the events table already keeps.
**Why:** The walk: "the run view is gone the moment the run succeeds — there is no terminal success run state to stand on", and dead end 9 — only the latest run renders and the runs counter goes nowhere. A terminal success state with a "Go to review" button was rejected as one extra click on the happy path of every run. Confirmed by the human 2026-09-04.

## 16. Burn copy sets a data-informed time expectation; zero-ticket states keep their honest behavior; a launch death gets a "launch failed" treatment
**Decision:** (a) **F25.1's fix stands**: both zero-ticket states (tickets phase and implementation-with-empty-ledger) keep their honest WAITING behavior, restyled onto the foundation's primitives. (b) **F12's other half lands in the bar**: the pre-burn copy states a time expectation, data-informed rather than hardcoded — the median of this project's past ticket durations when history exists ("tickets have been taking ~2 min each"), a generic "typically a few minutes per ticket" before any history. (c) **A lane that fails before first agent output gets a distinct "launch failed" treatment** — visually different from an agent failure: decision 13's verdict strip explains the sandbox never started, the raw `git`/`docker` error sits behind a disclosure, and a hint line appears when the error matches a known cause (e.g. long Windows paths). Retry stays available. The sandcastle path-rewrite fix itself remains parked per decision 9.
**Why:** These are the last of walk §A's findings owed a decision: F25.1 needed only reaffirmation, F12's rail copy was fixed but the bar still set no duration expectation (and a hardcoded number would rot — the project's own history is the honest source), and dead end 1 showed a run dying in ~1 s with a raw mount error and no framing. Confirmed by the human 2026-09-04.

## 17. The review page opens on one shared evidence stage: walkthrough player by default, the drive panel while driving
**Decision:** The top of the review page is a single large evidence stage — roughly full content width, 16:9 — not a card among cards and not a two-up split. By default it plays the **latest** review walkthrough (per decision 8's "latest review shows" rule); when the human starts a test drive it swaps to the Open-app drive panel (decision 7's integrated annotate-capable view if the capture prototype proves out, else an embedded iframe or a prominent `Open app ↗` with the same chrome); stopping the drive swaps back. A compact header above the stage carries the walkthrough's identity (`Walkthrough · 12:34 · reviewed <sha>`) and the `Open app ▶` affordance. When no walkthrough exists yet the stage is the drive panel with an honest "no walkthrough yet" note — the top of the page is never a dead card. Drive problems that are about the stage (setup failure, bare checkout) render *in* the stage where the video would be.
**Why:** The human chose the shared stage over a two-up split (2026-09-04). The complaint was clutter, and two-up re-creates it while capping the video at ~⅔ width against the "much bigger video" ask (decision 5 items 1–2). Watching the walkthrough and driving the app are the same activity — inspecting the build with annotation tools in hand — and never happen simultaneously, so they share one canvas; the swap also puts drive-state truth where the eye already is instead of in a card at y≈875.

## 18. Under the stage: alert slot → one-line status strip → open work → collapsed prose
**Decision:** The rest of the review page is four bands, in order. (a) **Alert slot** — ConflictCard-level interruptions render between the stage and the status strip, loudest thing on the page but never above the stage; alerts about the stage itself (drive setup failed) render in the stage per decision 17. (b) **A one-line status strip** of chips, the returning human's TL;DR: review outcome with its freshness stamp (decision 19), checks, `Lap N · X of Y tickets landed · Z waived`, run state. Each chip is a disclosure or anchor into its section. This strip is where decision 11's waived tickets surface as explicitly-unfinished work, and where "tickets N/N done" stops counting the review ticket as delivered work (walk §B dead end 3). (c) **Open work** — the OpenDefects list and the notes panel merge into one "what still needs attention" section (review-agent defects and drive/annotation notes share triage destinations), each row keeping its evidence thumbnail and timestamp; a `fixing` defect renders as "being fixed in the running burn" with a link to its lane, closing the research's invisible-`fixing` gap. (d) **Prose, collapsed** — the review digest, the burner digests and the lap account's narrative sit behind a single "Full accounts" disclosure at the bottom; the lap account's numbers live in the strip, its story in the prose. The standalone Summary card, the digest wall and PlannedNextLapCard-as-card dissolve into this structure (planned-next-lap becomes a line in the lap chip's disclosure). "No review ran this lap" is a strip-level amber state, not a buried row.
**Why:** Decision 6 ordered evidence → state → prose; this fixes the concrete bands. The walk's §B dead ends 1–3 (digest wall first, check rows below the fold, review ticket counted as delivered) all die with the strip, and the human confirmed the band structure 2026-09-04 with one amendment — the latest review's video and text are what shows — which is decision 19's subject.

## 19. Vouching is fixed by freshness-stamping, not lap-scoping; the server carries the evidence's moment
**Decision:** `reviewOutcome`/`reviewChecks`/`reviewWalkthroughUrl` keep picking the **latest** review pass — lap-scoping them would blank the stage the moment a lap flips, against decision 8. Instead the vouching bug is fixed by stamping: (a) the server adds `lap` and the reviewed commit to the review-artifacts listing (`ReviewTicketArtifacts`, `routes/reviews.ts` — the ticket rows already carry `lap`, so this is the one-line change the research costed) and to `reviewOutcome`; (b) the UI computes staleness from what has landed since the reviewed commit and the status strip says it plainly — fresh: `Reviewed ✓ · this build`; stale: amber `Reviewed 2 laps ago · 5 tickets landed since — evidence may be outdated`. The amber stamp subsumes `NO_REVIEW_ROW`: "no review ran this lap" becomes the limiting case of "the review you're looking at predates the current build". (c) `lapAccount` stays lap-scoped — it is a narrative of this lap, now living in decision 18's collapsed prose band, so the walked one-card self-contradiction (flow-map §F, lap-2 table) can no longer occur. (d) `Workspace.tsx` feeds the same stamped outcome to the merge dialog, so the merge status line inherits the honesty. The re-review waypoint decides *when* a fresh pass runs; this decision guarantees the page never lies about which build the evidence describes whether or not one has.
**Why:** Confirmed by the human 2026-09-04. The walked bug was lap 1's green "no findings" and lap 1's video presenting as lap 2's state; the naive lap-filter fix would trade a lie for a dead stage. Decision 8's rule — latest review of the current build, stamped with its moment — makes the stamp the fix and the filter the mistake.

## 20. Test-drive states derive from one server-side drive-state value; the bar and the stage can no longer disagree
**Decision:** A single drive-state value (`idle / starting / serving / bare-checkout / setup-failed / review-agent-driving`), computed server-side, drives both the next-step bar and the stage. State by state: **idle, dev command set** — the stage header carries `Open app ▶`; the stretched explainer card dies, replaced by one line under the header with full checkout/teardown detail behind a disclosure. **Serving** — the stage is the drive panel (decision 17); dev-server chip, branch and Show-output xterm in the stage footer; only this state gets the bar's "Test-driving the branch — merge when it looks right". **Bare checkout (no dev command)** — the stage says "Branch checked out — nothing started. This project has no dev command · Set one in Settings", and the bar reads "Branch checked out for inspection" with Stop primary — never the merge copy (closes walk §C dead end 2). **Setup failed** — `DRIVE SETUP FAILED` · command · exit code · output behind a disclosure · **Fix drive**, rendered in the stage; the bar reads "Drive setup failed — fix it or stop the drive" (closes the §G finding that the bar never learns). **Review agent driving** — the stage shows the agent's drive with a live "review agent driving — notes land below as it finds things" banner and **Stop the review drive**; the stop stays purpose-blind (recovery for a dead agent holding the branch) and Start-over-it keeps the toast. **Iterate during a drive** stays disabled-with-reason (F3's fix stands), and the reason line gains a one-click **"Stop drive and iterate"** so the dead end becomes one button.
**Why:** The walk caught the bar lying twice — "merge when it looks right" over a bare checkout and over a failed setup — because the bar and the card derived drive truth independently. One shared state value makes the disagreement structurally impossible, and putting failure where the video would be uses the stage's prominence for the state that most needs it. Confirmed by the human 2026-09-04.

## 21. One door forward: the bar offers Merge & ship or Iterate; triage lives inside the Iterate door
**Decision:** The review bar's forward choices collapse to two decisions: **Merge & ship** (it's right) or **Iterate** (keep working). "Address notes" disappears as a bar verb. Iterate always opens the same triage step listing whatever open notes and defects exist; each row can be marked *quick fix* (becomes a ticket) or *carry into the conversation*. Two exits: everything quick-fixed (nothing to discuss) → tickets mint and the burn starts with no lap session opened; anything left to discuss, or the user wants to talk → the lap session opens with those notes carried in. **Zero notes: the triage step is skipped entirely** — Iterate goes straight to the lap conversation empty-handed. "Fix" survives as a capability (tickets can burn without a conversation) but stops being a separate route. The triage step's internals (row design, images, lap-scoping of the list) are the annotation/triage waypoint's to grill; what is locked here is the route: two bar decisions, one iterate door, empty-notes passthrough.
**Why:** The human, 2026-09-04: the review page had "two routes — click Iterate and talk to the agent, or write test notes and convert them to tickets — it would make more sense as one route; with no notes, proceed with it empty." Address-notes and Iterate were the same decision ("this isn't ready") entered through two doors, with the Fix/Iterate fork duplicated inside the dialog; one door removes the duplicate choice and puts the notes in front of the human at the moment they choose.

## 22. An annotation stays a baked PNG, but its timestamp is bound to its recording; an orphaned note keeps its picture and loses its live seek
**Decision:** The saved annotation remains one flat PNG (frame + strokes composited client-side) — no stroke persistence, no attachments table. What changes: a note's `videoTimestamp` is bound to the recording it was taken against (the note stores the source review ticket/artifact id, one nullable column). The rule: **a timestamp only ever seeks the video it was recorded against; otherwise the PNG is the evidence.** While the note's own recording is on the stage, the timestamp is a live jump. When re-review (decision 8) has replaced the recording — which makes "a note on a video that no longer exists" the *normal* case, every pass orphans the previous notes' seeks — the timestamp renders as a plain label (`0:42 · earlier walkthrough`) and clicking the row's evidence opens the PNG in the in-app viewer instead of seeking. Old recordings stay on disk per review ticket, so a later "view in its own recording" needs no schema change.
**Why:** The unbound timestamp is the one genuinely broken part of the data model — walked live on lap 2, where a lap-1 note's `0:42` seeks lap 2's video to an unrelated frame. The baked PNG is what makes the downstream chain work (thumbnail, triage evidence, `.runcastle-attachments/<id>.png` into the burner) and is self-contained against any video replacement; persisting vectors would add a table and a renderer everywhere for a post-save-editing capability none of the confirm session's issues asked for. Confirmed by the human 2026-09-04.

## 23. The player's control surface: keyboard-first transport, frame-stepping for accuracy, honest loading and error states
**Decision:** The player is sized for its actual job — an AI browser-use walkthrough watched at 1.5× and paused on the exact frame of a defect — not for a general video site. (a) **Transport:** clicking the frame toggles play/pause; Space and K do the same when the human is not typing; J/L or ←/→ jump 5 s; `,`/`.` step one frame (~1/30 s) while paused; `<`/`>` cycle playback speed (0.5–2×, default 1.5× per decision 5), shown as a `1.5×` control in the bar; F fullscreen. No volume/mute — walkthroughs carry no audio worth controlling. (b) **Scrub:** the slider is coarse navigation with a hover tooltip showing the target time; slider arrow keys move 1 s (not 0.05 s). Frame-exactness is `,`/`.`'s job — no fine-seek zoom, no filmstrip/thumbnail preview. (c) **Loading:** the stage shows the first-frame poster as soon as one exists, with a spinner and honest copy ("Loading the recording — 21 MB") until seekable; controls are disabled but visibly *loading*, never dead. (d) **Error:** a decoder error flips the stage to an explicit broken state — "This recording can't be played (file may be corrupt)" with the file path and a retry — never the healthy-but-empty look; Play never "refuses via toast". (e) **Layout:** the stage and its control bar are one unit that fits the viewport together; annotating never requires scrolling the frame out of view (the walked sticky-bar overlap dies with decision 17's stage-on-top layout).
**Why:** The walk found no keyboard at all, 0.05 s slider arrows (20 presses per second), 2.1 s per slider pixel at 20 min making a given second unreachable, no frame step, a 31-second dead-looking card while metadata loaded, and a corrupt file rendered identically to a loading one forever. Frame-stepping plus 5 s jumps covers "find the flicker" with far less machinery than a fine-seek slider. Confirmed by the human 2026-09-04.

## 24. Annotation capture: pen + arrow + rectangle, save gated on *any* content, multi-line text, confirmed discards; Annotate is never disabled
**Decision:** (a) **Entering:** annotation stays pause-gated, but the Annotate button is never disabled-with-tooltip — while playing it stays clickable, and clicking it pauses on the current frame and opens the overlay in one act; "pause first" becomes behavior, not a greyed control. (b) **Tools:** pen, arrow, and rectangle — circle it, point at it, box the region. One colour (danger red), one width, drawn ~6 px on-screen so it reads at stage size. Undo/redo per stroke, Clear. No eraser (undo covers it), no on-frame text tool (the note text is where words go), no colour picker. (c) **Save gating flips:** a drawing alone saves fine — text is optional; a text-only save stays legal and bakes the bare frame as today. Save is disabled only when there is *neither* a stroke nor text. (d) **Note text is a multi-line textarea:** Enter saves, Shift+Enter inserts a newline. (e) **Discarding:** Escape or Cancel over a dirty overlay (any stroke or text) asks one confirm ("Discard this annotation?"); a clean overlay closes silently. No draft persistence — an annotation is a 20-second act. (f) The capture path must survive arbitrary strokes — the walked second-stroke crash (decision 3's root cause) is fixed as part of this rebuild, not as a separate patch.
**Why:** The walk's dead ends 1, 3, 4, 5 and 9–10: a crash on stroke two, a drawing that cannot be saved without typing, a single-line input where Enter fires, silent discards with Escape bound inside the text box, and a disabled-with-hover Annotate — the exact "randomly not existing" pattern ux-issues dec. 10 removed elsewhere. Arrow + rectangle earn their place over pen-only-but-thicker because an AI-walkthrough defect is usually "this element, here", and freehand circling was imprecise even at 788 px. Confirmed by the human 2026-09-04.

## 25. The notes list: evidence-forward rows, bidirectional visible jumps, timeline markers, in-place edit, confirmed delete, lightbox, one image per note
**Decision:** Within decision 18(c)'s merged "open work" section: (a) **Row anatomy:** larger thumbnail (~96×54), timestamp chip (live jump when its recording is on stage, `0:42 · earlier walkthrough` label otherwise, per decision 22), full multi-line note text, author chip, a lap badge when the list spans laps, Edit/Delete. (b) **Jumps are bidirectional and visible:** clicking a timestamp seeks *and* smooth-scrolls the stage into view; saving an annotation scrolls the list to the new row and briefly highlights it — neither direction changes state off-screen. (c) **Timeline markers:** the scrub bar carries a dot at each note's timestamp for the recording currently on stage; notes within ~1 s merge into one dot with a count; clicking a dot seeks there and highlights its note(s) in the list. Five notes on one frame are one counted dot and five rows. (d) **Edit in place:** the row expands with the same multi-line textarea; thumbnail and timestamp stay visible throughout; the baked picture is not editable. (e) **Delete confirms** ("Delete this note and its picture?") on the foundation dialog primitive; no undo beyond it. (f) **In-app lightbox** for thumbnails (full-size PNG, Escape/click-out closes) — never a raw-PNG browser tab. (g) **Pasted images (decision 7a):** the composer and edit state accept paste/attach onto the existing note-PNG pipeline. **One image per note** — a video annotation's is the baked frame, a hand note's is the pasted screenshot; pasting onto a note that has one replaces after a confirm; multiple pictures are multiple notes.
**Why:** Walk dead ends 11–15: jumps that move the playhead off-screen, edits that hide the evidence being edited, an unconfirmed destructive delete, and a raw-PNG tab as the only viewer. Timeline markers make the video the index of its notes rather than just their source, and resolve the many-notes-one-frame case without list clutter. One image per note preserves the PNG-keyed-by-note-id storage model and pushes "many pictures" toward many notes, which triage handles better. Confirmed by the human 2026-09-04.

## 26. Triage internals: evidence-forward rows, per-row Quick fix / Discuss defaulting to Discuss, honest footer, a review debt that is owed, live list, visible failure
**Decision:** Inside decision 21's Iterate door: (a) **Rows reuse decision 25's anatomy** — thumbnail with working lightbox, timestamp as a label (not a live jump — the step doesn't drive the player behind itself), full text, author chip, lap badge — grouped by lap when the open work spans laps. (b) **One per-row control: a Quick fix checkbox, unchecked by default.** "Discuss" is not a per-note act — every note not quick-fixed (and not dismissed) rides into the *one* lap conversation together, as a set. The footer is the step's summary ("2 tickets will mint · 4 notes carried into the lap conversation"); with nothing checked the step is simply "review what you're bringing to the conversation → Start lap N+1". A "mark all as quick fixes" affordance exists; a row can also be dismissed behind the same delete confirm. Unchecked is the default because minting a ticket is the mechanical act — the human opts into it per row rather than un-checking a pre-checked batch. (c) **Ticket titles stop being truncated text:** title = the note's first sentence cut at a word boundary, no "…" in the title; full text, attachment path, `m:ss` clock and Read instruction stay in the ticket context. (d) **The footer states what happens next honestly**, including standing debt: when older pending fix tickets exist, "N unburned fix tickets from lap M will burn with these" (closes the walked silent-ride-along). (e) **Invariant: a quick-fix batch never returns to review unreviewed.** Decision 8's re-review pass is the mechanism; whether it is an appended review ticket or a triggered pass is the re-review waypoint's call — the invariant is locked here. (f) **The list is live:** a note saved while the step is open appears with a brief highlight, defaulting to Discuss; after the step commits, a new note is ordinary open work for the next round. (g) **A failed Iterate renders in the alert slot** (decision 18a): "Lap N+1 couldn't start" + the git error behind a disclosure + Retry — never only in the Activity feed.
**Why:** The walk's triage dead ends 1–6: the picture that justified a note was invisible exactly where the decision is made; titles carried a literal ellipsis; batch-promote burned and returned unreviewed; laps mixed unlabelled; pending fixes rode into the next lap silently; a failed Iterate looked like nothing happened. Confirmed by the human 2026-09-04, including keeping unchecked-carries-to-the-conversation as the default; the human's own framing — "I'll never discuss one note at a time; it's all at the same time" — is what reduced the control to a single checkbox.

## 27. The lap banner dies into the lap chip with tense-accurate copy; carried notes leave open work at the moment they are carried
**Decision:** (a) **The standing lap banner is removed.** The lap's position lives in decision 18's status-strip chip (`Lap 2 · 1 of 2 tickets landed`) and its story in the chip's disclosure, with copy driven by actual state: while the lap session has not run, "Lap 2 is open — its session will digest your notes and emit this lap's tickets"; only after it runs, the past tense. "Landed N" is computed per lap at burn-terminal from that lap's implementation tickets only — the review ticket never counts, and a lap-1 ticket that burns during lap 2 counts where it landed, with the disclosure saying so ("includes 2 fixes promoted in lap 1"). (b) **Carried notes stop being open work at the moment the triage step commits:** they move to a "carried into lap N" state — out of the open-work section and all future triage lists, into a collapsed group beneath it, each row keeping its evidence and gaining the lap link; a human can reopen one the lap didn't actually address. Quick-fixed notes keep the frozen-with-ticket-link treatment. When lap 2 comes back, open work contains only what is genuinely unaddressed: the lap-2 review pass's findings (stamped `this build` per decision 19) plus anything reopened or newly taken. Carrying is the addressing decision — the human made it at triage; "the lap session closes the notes it addressed" (agent judgment) can layer on later if wanted, but the mechanical rule is the floor.
**Why:** The walk's lap dead ends: a past-tense banner rendered before the session ran, "landed 4" drifting to "landed 6", the review ticket counted as delivered work, and seven stale open notes mixing into every future triage — the "confusing and outdated" surface of decision 5 item 7. Confirmed by the human 2026-09-04.

## 28. Loop remainders: the Burn count keeps counting all pending but says whose it is; the trust prompt and the drawn/bare distinction are parked
**Decision:** (a) **The Burn count stays whole-feature but gets labelled:** burning every pending ticket across laps is correct (ADR-0010 accumulates on the branch); when laps mix, the bar says so — "Burn 4 tickets — 2 from lap 2 · 2 carried from lap 1" — alongside decision 14(d)'s lap-grouped lanes and 26(d)'s footer honesty. (b) **Parked: the Claude Code "trust this folder" prompt** on spawned sessions (lap, resolve-conflict, drive-fix, Q&A) — the human reports hitting it only once, not routinely; not worth launcher work in this flow. Revisit if it recurs. (c) **Parked: distinguishing a text-only annotation from a drawn one** — with decision 24(c) a text-only save still bakes the bare frame, so the note carries a real picture and timestamp; no decision changes on the distinction.
**Why:** Decision 9 requires every walked dead end to get a decision or an explicit park; these are the loop's last three. The Burn-count bug was the missing label, not the count. Confirmed by the human 2026-09-04.

## 29. The merge dialog sees the conflict: a red row on top, a flipped primary, retry kept enabled
**Decision:** The `MergeFeatureDialog` derives from the same conflict truth as the next-step bar. Over a standing conflict the dialog's loudest element is a red block row at the top of "what lands" — "⚠ A merge conflict is standing (`index.html`) — this merge will fail unless it's been resolved." — its primary flips to **Resolve the merge conflict** (the same act as the ConflictCard's button), and **Retry merge anyway** stays as an enabled secondary with honest copy ("if you resolved it by hand, retry lands it"). Nothing is disabled: `fix-merge-conflict-system` decisions 2b/3 stand — a disabled merge deadlocked hand-resolved conflicts runcastle could not see, and the best-effort `merge.resolved` probe can miss a real resolution. The green rows render below the red row so the human still sees what lands *if* it lands. Two riders: (a) the dialog's review row inherits decision 19's freshness stamp (19d already routes the stamped outcome to the dialog); (b) the warnings box names waived tickets (decision 11) — the merge dialog is the last catch for explicitly-unfinished work.
**Why:** The walk closed F8 at the bar but found the retry dialog blind — an all-green "what lands" over a branch that will re-conflict, its only warning the open-notes count. Deriving dialog and bar from one truth makes it impossible to read the dialog and not know about the conflict, while keeping the never-disabled guarantee that the hand-resolved human depends on. Confirmed by the human 2026-09-04.

## 30. The conflict state retires on ship; the card moves to the alert slot; `relTime` stops saying "now ago"; the bar's collapse is a bug ticket
**Decision:** (a) **A successful merge retires the conflict**: `feature.merge` on success clears the standing conflict (emit `merge.resolved` alongside `feature.shipped`, or make `unresolvedMergeConflict` treat shipped as terminal — implementation's pick; the invariant is *a shipped feature has no live conflict state*). This is the root fix for the conflict card haunting the read-only view; decision 33's readonly gate becomes belt-and-braces. (b) **The ConflictCard renders in decision 18(a)'s alert slot** — loudest thing on the page, below the stage — restyled on the foundation's primitives with its walked anatomy (what conflicted, file list, **Resolve with agent** primary; the retry path stays reachable via the bar per decision 29). (c) **The `relTime` "now" + " ago" concatenation is fixed once in the shared helper**, killing "recorded now ago" on the card and "merged now ago" on the shipped hero together. (d) **Best-effort resolution detection stays** (teardown outranks detection; Retry is the guaranteed path), but the card gains one honest state after a resolve session ends without clearing it: "The resolve session ended but the merge hasn't landed — resolve by hand or retry." — today the card silently stands, reading as the button having done nothing. (e) **The next-step bar's one-word-per-line collapse while the ConflictCard is up is a straight bug ticket** — the bar renders its kick/title/desc at full width regardless of which cards are mounted; no design decision needed.
**Why:** The research found the mechanism (`feature.merge` emits neither `burn.started` nor `merge.resolved`, so `unresolvedMergeConflict` returns the old conflict forever), the walk found the copy bug and the layout break, and the resolve-session-ended-silently case was walked live (§G). Confirmed by the human 2026-09-04.

## 31. The merge dialog's green case: stamped rows, an enumerated warnings box, a "what happens next" line
**Decision:** F21's read-to-confirm shape stands — no type-to-arm, Esc/backdrop close. The content sharpens: (a) **Rows**, each ✓/amber and never silently absent — *what lands*: "12 commits · 9 files" (files-changed from `git diff --stat`; the scale a human actually senses); *run*: "8/8 tickets done", with waived/failed counts rendered amber when nonzero, never green over set-aside work; *test drive*: taken / not taken; *review*: decision 19's stamped outcome ("Reviewed ✓ · this build" / amber "reviewed 2 laps ago · 5 tickets landed since"). (b) **The warnings box enumerates exactly**: open notes count, waived tickets, standing unburned fix tickets from earlier laps (decision 26(d)'s debt, surfaced at the last catch too), and stale review evidence whenever the review row is amber. (c) **One "what happens next" line at the bottom**: "Merges `feature/x` into `main`, writes the outcome doc, and moves the feature to Shipped." — the last moment to say what the button does, and it sets up decision 32's post-merge outcome doc.
**Why:** The walked dialog was already the right shape; what was missing was the full inventory (so converge doesn't guess), amber honesty for waived work, and the freshness stamp on the row most likely to lie. Confirmed by the human 2026-09-04.

## 32. The outcome doc is written after the merge lands, on `main`, and synthesized — never the digest wall
**Decision:** (a) **Timing**: the outcome doc is committed to `main` in its own `runcastle: outcome for <slug>` commit immediately after the merge commit, dated at that moment. It cannot claim `Shipped:` early because it does not exist until shipped is true. The trade-off is accepted: the doc lands beside the merge rather than riding it — `main` is the record. (b) **Content is synthesized, not transplanted**: header (feature one-liner, `Shipped:` date, laps run); *what shipped* (commit/file scale, tickets landed per lap, waived/failed noted honestly); *review record* (each lap's review outcome with its freshness stamp, the latest pass's findings and how they were resolved — written from the stamped data, so the walked frozen cross-lap contradiction cannot recur); *notes record* (what the human flagged and where each went — quick-fixed, carried, dismissed); then per-ticket digests **filtered** — "Surprises: None / Left undone: Nothing" rows dropped, only tickets with something to say get a section, grouped by lap. Evidence and state first, prose last, in the permanent record too (decision 5 item 4 applies to the doc). (c) The shipped hero links the doc, and the shipped view's read-only stage plays the final walkthrough — the recording is part of the record. The lap banner's death (decision 27) covers the shipped view; the `merged now ago` copy dies with decision 30(c).
**Why:** The walk caught the doc committed as a *parent of the merge* — stamped `Shipped:` while the first merge attempt sat conflicted in review — and reprinting the burner-digest noise verbatim ×8, laps mixed, as the permanent record. Writing after the merge makes early claims structurally impossible; synthesis makes the record the readable account the digest wall never was. Confirmed by the human 2026-09-04.

## 33. Read-only is a structural rule; Q&A conversations leave a record
**Decision:** (a) **`readonly` means no live actions anywhere** in the redesigned ReviewBody — a rule of the layout, not a per-card patch. The stage plays the final walkthrough with Annotate gone; the status strip and open-work rows render with their evidence but no triage controls; action cards (ConflictCard, DriveFailureCard, StopReviewDrive — the three the research found ignoring the flag) either render a neutral historical statement or not at all. With decision 30(a) a shipped feature has no live conflict state, so the live-conflict-card case is unreachable and the gate is belt-and-braces. The read-only Test-drive card states what *was* done ("test drive taken · lap 2"), never an instruction. (b) **The shipped view keeps a Q&A history** under the hero: each conversation is a row — opening question as the title when a transcript exists, timestamp, expandable transcript. A session that ended without capturing a transcript leaves a minimal row ("session opened · nothing recorded") rather than vanishing — nothing on a shipped feature's record silently disappears. The trust-prompt friction stays parked per decision 28(b).
**Why:** The walk confirmed the `ux-issues` item live (conflict card gated on `conflict &&`, never `readonly`, with a live agent-launching button in a history view) plus two more of the same family, and verified an ended Q&A vanishing on reload with no list of questions ever asked. A structural rule survives the ReviewBody split (the 971-line file is being rebuilt anyway); a per-card patch is how the inconsistency happened the first time. Confirmed by the human 2026-09-04.
