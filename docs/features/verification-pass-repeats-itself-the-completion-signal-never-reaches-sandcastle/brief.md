# Verification pass repeats itself — the completion signal never reaches sandcastle

## Ticket 1

Fix packages/skills/burner/verify-fixes.md so the completion signal reliably lands in stdout. Today line 39 tacks 'End with <promise>COMPLETE</promise>' onto the digest-writing paragraph, which reads as 'end the DIGEST file with it' — a marker inside the file is invisible to sandcastle, which only scans agent stdout, so the iteration loop re-runs the same prompt (observed: a verification pass re-derived everything 3 times, full test suite each time, re-reporting findings each pass). Give the template the same 'Signal completion' hard rule its siblings carry (implement-ticket.md:15, review-ticket.md:26): print exactly <promise>COMPLETE</promise> as the last line of the final MESSAGE, whether the pass succeeded or was blocked — and, matching review-ticket.md:184, say the marker must never appear inside DIGEST.md itself. Rephrase line 39 so it cannot be read as ending the file with the marker.

## Ticket 2

Belt-and-braces in packages/server/src/workflows/review-ticket.ts: stop the sandcastle iteration loop once the digest exists, so a missed completion signal costs one iteration, not burnMaxIterations full re-derivations. Mechanism: onAgentStreamEvent events carry the iteration number (see docs/research/SANDCASTLE-NOTES.md — no onIteration hook exists, stream events are the iteration boundary); when an event for iteration >= 2 arrives and artifacts.digestPath already exists on disk, fire the ticket's own abort (ticketAbort, already registered via registerTicketAbort). The outcome harvest already reads success off files: runError set + digest present returns { status: 'done' } (reviewTicketOutcome ~line 467-481), so this ends cleanly rather than failing the ticket. Guard: only for review/verification execution (this file), never the implementation path where multi-iteration continuation is the point. Unit-test the pure decision (given iteration number + digest-exists, abort or not); the sandcastle boundary itself stays untested per the file's existing convention.
