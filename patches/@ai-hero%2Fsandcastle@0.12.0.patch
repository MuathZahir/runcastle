diff --git a/dist/MountConfig-CmXclHA5.d.ts b/dist/MountConfig-CmXclHA5.d.ts
index 9ad1fdd590d0b19998e2c55eb6a412dce165028c..cd43332576d0d4040b1ef7611a0147acf6ab8585 100644
--- a/dist/MountConfig-CmXclHA5.d.ts
+++ b/dist/MountConfig-CmXclHA5.d.ts
@@ -3,8 +3,8 @@
  *
  * Each entry describes a host directory to mount into the sandbox container.
  */
-/** A single bind-mount descriptor for docker()/podman() providers. */
-interface MountConfig {
+/** A host directory bind-mounted into the sandbox. */
+interface HostPathMountConfig {
     /**
      * Path on the host. Supports:
      * - Absolute paths (`/data/cache`)
@@ -21,6 +21,25 @@ interface MountConfig {
     readonly sandboxPath: string;
     /** Mount as read-only. Defaults to `false`. */
     readonly readonly?: boolean;
+    readonly volume?: undefined;
 }
+/**
+ * runcastle patch — an engine-managed named volume mounted into the sandbox.
+ *
+ * A named volume is owned by Docker/Podman, not the host filesystem, so there is
+ * no host path to expand, resolve or stat; the engine creates it on demand. The
+ * `-v` flag the providers already emit accepts `<name>:<path>` verbatim.
+ */
+interface VolumeMountConfig {
+    /** Docker/Podman volume name, emitted as `-v <volume>:<sandboxPath>`. */
+    readonly volume: string;
+    /** Path inside the sandbox container — same resolution rules as a host-path mount. */
+    readonly sandboxPath: string;
+    /** Mount as read-only. Defaults to `false`. */
+    readonly readonly?: boolean;
+    readonly hostPath?: undefined;
+}
+/** A single mount descriptor for docker()/podman() providers. */
+type MountConfig = HostPathMountConfig | VolumeMountConfig;
 
 export type { MountConfig as M };
diff --git a/dist/chunk-62WN33RK.js b/dist/chunk-62WN33RK.js
index 014dcef940e6eec6b1334a0bef1ffa1fdbea9c84..4ff8560a5c3332d7289af6c80fd0240db273d81d 100644
--- a/dist/chunk-62WN33RK.js
+++ b/dist/chunk-62WN33RK.js
@@ -30,6 +30,11 @@ var noSandbox = (options) => ({
             ],
             windowsVerbatimArguments: isWindows
           });
+          // runcastle patch: report the child so the caller has a handle to kill
+          // by when a run is aborted — nothing here keeps a reference otherwise,
+          // and a fresh child is spawned per exec. `pid` is undefined when the
+          // spawn itself failed, and there is nothing to kill then.
+          if (proc.pid !== void 0) options?.onChildSpawn?.(proc.pid);
           if (opts?.stdin !== void 0) {
             proc.stdin.write(opts.stdin);
             proc.stdin.end();
@@ -84,6 +89,8 @@ var noSandbox = (options) => ({
             stdio: [opts.stdin, opts.stdout, opts.stderr],
             shell: process.platform === "win32"
           });
+          // runcastle patch — same handle, the interactive spawn.
+          if (proc.pid !== void 0) options?.onChildSpawn?.(proc.pid);
           proc.on("error", (error) => {
             reject(new Error(`exec failed: ${error.message}`));
           });
diff --git a/dist/chunk-CP3TYXZA.js b/dist/chunk-CP3TYXZA.js
index 7d097321871cfee95a8ba7e6d12a7fe002190cc5..23a68bed8276ffdf481644e10b0822d41082c969 100644
--- a/dist/chunk-CP3TYXZA.js
+++ b/dist/chunk-CP3TYXZA.js
@@ -130,7 +130,11 @@ var docker = (options) => {
     env: options?.env,
     sandboxHomedir,
     create: async (createOptions) => {
-      const containerName = `sandcastle-${randomUUID()}`;
+      // runcastle patch: a caller may name the container itself, so it has a
+      // handle to kill by (`docker rm -f <name>`) when a run is aborted —
+      // sandcastle's own abort never reaches the container. Omitted, the
+      // generated `sandcastle-<uuid>` name is used exactly as before.
+      const containerName = options?.containerName ?? `sandcastle-${randomUUID()}`;
       const worktreePath = createOptions.mounts.find(
         (m) => m.hostPath === createOptions.worktreePath
       )?.sandboxPath ?? "/home/agent/workspace";
@@ -138,7 +142,11 @@ var docker = (options) => {
       const volumeMounts = allMounts.map((m) => ({
         hostPath: m.hostPath,
         sandboxPath: m.sandboxPath,
-        readonly: m.readonly
+        readonly: m.readonly,
+        // runcastle patch: carry the named-volume marker through to
+        // formatVolumeMount, which suppresses the SELinux label for one. Podman
+        // passes the resolved mount straight through and needs no equivalent.
+        volume: m.volume
       }));
       const imageName = configuredImageName ?? defaultImageName(createOptions.hostRepoPath);
       const containerUid = options?.containerUid ?? process.getuid?.() ?? 1e3;
diff --git a/dist/chunk-NGBM7T3E.js b/dist/chunk-NGBM7T3E.js
index c2b77a43fca309822bd882256dc4c593af75bac0..31ca930d815d816985487af6aae257b183455c3a 100644
--- a/dist/chunk-NGBM7T3E.js
+++ b/dist/chunk-NGBM7T3E.js
@@ -71,5 +71,40 @@ var BoundedTail = class {
   }
 };
 
-export { BoundedTail, MAX_TAIL_CHARS, __commonJS, __export, __reExport, __require, __toESM };
+// runcastle patch — how a failed sandbox command reports itself.
+// See docs/adr/0011-sandcastle-patch-is-permanent.md.
+//
+// Upstream renders `Command failed (exit N): <command>\n<stderr>`, which loses a
+// setup failure twice over. Maven, Gradle and most JVM build tools write their
+// diagnostics to STDOUT, so a stderr-only message turns a failed dependency
+// install into a bare "exit 1" with nothing to read at all. And the burn's
+// onSandboxReady hook delivers its files as base64 payloads, so echoing the
+// command whole buries whatever diagnostic there is under kilobytes of noise.
+//
+// Lives here, next to MAX_TAIL_CHARS, because the combined tail is bounded by
+// the same budget `maxOutputTailChars` gives the streamed output it comes from.
+
+/**
+ * `command` with the body of each long single-quoted base64 literal replaced by
+ * an ellipsis — the `printf %s '<blob>' | base64 -d > <file>` shape the burn's
+ * guard install command writes its files with. Keying on the quotes and the
+ * base64 alphabet rather than on length alone is what keeps a long URL, JSON
+ * argument or generated id — any of which may BE the diagnostic — in the
+ * message.
+ */
+var elidePayloadTokens = (command) => command.replace(/'[A-Za-z0-9+\/=]{120,}'/g, "'…'");
+
+/** The message a non-zero exit reports: what ran, how it exited, what it said. */
+var formatExecFailureMessage = (command, result) => {
+  const output = [result.stdout, result.stderr]
+    .map((stream) => (stream ?? "").trimEnd())
+    .filter((stream) => stream.length > 0)
+    .join("\n");
+  const tail = output.length > MAX_TAIL_CHARS ? output.slice(output.length - MAX_TAIL_CHARS) : output;
+  const headline = `Command failed (exit ${result.exitCode}): ${elidePayloadTokens(command)}`;
+  return tail ? `${headline}
+${tail}` : headline;
+};
+
+export { BoundedTail, MAX_TAIL_CHARS, __commonJS, __export, __reExport, __require, __toESM, formatExecFailureMessage };
 //# sourceMappingURL=chunk-NGBM7T3E.js.map
diff --git a/dist/chunk-VOG34SRF.js b/dist/chunk-VOG34SRF.js
index 93f24d139b795c284915a10c34d82f2fa769eab0..2317d00d74747b4edaa89102230018788870548d 100644
--- a/dist/chunk-VOG34SRF.js
+++ b/dist/chunk-VOG34SRF.js
@@ -1,5 +1,5 @@
 import { createRequire } from 'node:module';
-import { __export } from './chunk-NGBM7T3E.js';
+import { __export, formatExecFailureMessage } from './chunk-NGBM7T3E.js';
 import * as clack from '@clack/prompts';
 import { join as join$1, basename, dirname, resolve, posix, normalize, isAbsolute } from 'path';
 import { styleText, promisify } from 'util';
@@ -25824,8 +25824,8 @@ var execOk2 = (sandbox3, command, options) => Effect_exports.flatMap(
     new ExecError({
       command,
       exitCode: result.exitCode,
-      message: `Command failed (exit ${result.exitCode}): ${command}
-${result.stderr}`
+      // runcastle patch — stdout as well as stderr, and no payload echo.
+      message: formatExecFailureMessage(command, result)
     })
   ) : Effect_exports.succeed(result)
 );
@@ -26821,6 +26821,21 @@ var resolveSandboxPath = (sandboxPath, sandboxHomedir) => {
   return isAbsolute(expanded) ? expanded : resolve(SANDBOX_REPO_DIR, expanded);
 };
 var resolveUserMounts = (mounts, sandboxHomedir) => mounts.map((m) => {
+  // runcastle patch: a mount entry may name a Docker/Podman *named volume*
+  // instead of a host directory. A volume is owned by the container engine, so
+  // there is no host path to expand, resolve or stat — and the engine creates
+  // it on demand, so the existence check below would reject a perfectly valid
+  // mount. The name is carried through as both `hostPath` (what the `-v` flag
+  // wants on the left of the colon) and `volume` (the marker formatVolumeMount
+  // reads). Host-path mounts fall through and behave exactly as before.
+  if (m.volume !== void 0) {
+    return {
+      hostPath: m.volume,
+      sandboxPath: resolveSandboxPath(m.sandboxPath, sandboxHomedir),
+      volume: m.volume,
+      ...m.readonly ? { readonly: true } : {}
+    };
+  }
   const resolvedHostPath = resolveHostPath(m.hostPath);
   if (!existsSync(resolvedHostPath)) {
     throw new Error(
@@ -26937,7 +26952,12 @@ var patchGitMountsForWindows = (gitMounts, worktreeHostPath, sandboxRepoDir, rea
 });
 var formatVolumeMount = (mount, selinuxLabel) => {
   const base = `${mount.hostPath}:${mount.sandboxPath}`;
-  const options = [mount.readonly ? "ro" : void 0, selinuxLabel || void 0].filter((option5) => option5 !== void 0).join(",");
+  // runcastle patch: SELinux relabelling is a bind-mount concern. An
+  // engine-managed named volume is created with the correct label already, and
+  // `:z` on one asks the engine to chcon the volume's whole tree every time a
+  // container starts — ruinous for a multi-GB dependency cache.
+  const selinux = mount.volume !== void 0 ? void 0 : selinuxLabel || void 0;
+  const options = [mount.readonly ? "ro" : void 0, selinux].filter((option5) => option5 !== void 0).join(",");
   return options ? `${base}:${options}` : base;
 };
 var processFileMountParents = (mounts, sandboxHomedir, statFn = statSync) => {
diff --git a/dist/index.d.ts b/dist/index.d.ts
index e451924caf673166b63daf652a056412e6025552..779ffc873a1b454783a1d604b54dfc66987ead31 100644
--- a/dist/index.d.ts
+++ b/dist/index.d.ts
@@ -1048,4 +1048,15 @@ interface CwdError extends Error {
 /** The provided `cwd` path does not exist or is not a directory. */
 declare const CwdError: CwdErrorConstructor;
 
+/**
+ * runcastle patch — the message a failed sandbox command reports.
+ * See docs/adr/0011-sandcastle-patch-is-permanent.md.
+ *
+ * Names the command with its base64 payloads elided, and appends stdout as well
+ * as stderr (bounded together) — a JVM toolchain writes its diagnostics to
+ * stdout, so stderr alone renders a failed setup as a bare "exit 1".
+ */
+declare const formatExecFailureMessage: (command: string, result: ExecResult) => string;
+export { formatExecFailureMessage };
+
 export { type AgentCommandOptions, type AgentProvider, type AgentStreamEvent, AnySandboxProvider, BindMountSandboxHandle, BranchStrategy, type ClaudeCodeOptions, type CloseResult, type CodexOptions, type CopilotOptions, type CreateSandboxOptions, type CreateWorktreeOptions, type CursorOptions, CwdError, ExecResult, type HostSessionLookup, type InteractiveOptions, type InteractiveResult, type IterationResult, type IterationUsage, type LoggingOption, MergeToHeadBranchStrategy, NamedBranchStrategy, type OpenCodeOptions, Output, type OutputDefinition, type OutputObjectDefinition, type OutputStringDefinition, type PiOptions, type PrintCommand, type PromptArgs, type ResumeSandboxRunResultOptions, type RunOptions, type RunResult, type Sandbox, type SandboxExecOptions, type SandboxHooks, type SandboxInteractiveOptions, type SandboxInteractiveResult, SandboxProvider, type SandboxRunOptions, type SandboxRunResult, StructuredOutputError, type Timeouts, type Worktree, type WorktreeBranchStrategy, type WorktreeCreateSandboxOptions, type WorktreeInteractiveOptions, type WorktreeRunOptions, type WorktreeRunResult, claudeCode, claudeHostSessionPath, claudeSandboxSessionPath, codex, copilot, createSandbox, createWorktree, cursor, encodeProjectPath, findClaudeSessionOnHost, findCodexSessionOnHost, interactive, opencode, pi, run, transferClaudeSession, transferCodexSession };
diff --git a/dist/index.js b/dist/index.js
index 2a1d6d7e902e34620d64929418b34c715ab65d7d..b2927e71168cd6a25a61639769148df283aaa55a 100644
--- a/dist/index.js
+++ b/dist/index.js
@@ -4,6 +4,9 @@ import { Context_exports, CwdError, Effect_exports, resolveCwd, getCurrentBranch
 export { createBindMountSandboxProvider, createIsolatedSandboxProvider } from './chunk-BIWNFKGV.js';
 import { noSandbox } from './chunk-62WN33RK.js';
 import './chunk-NGBM7T3E.js';
+// runcastle patch — the failure-message renderer, exported so its contract can
+// be pinned by a test. See docs/adr/0011-sandcastle-patch-is-permanent.md.
+export { formatExecFailureMessage } from './chunk-NGBM7T3E.js';
 import { mkdirSync, appendFileSync } from 'fs';
 import path, { join, posix, dirname, relative } from 'path';
 import { styleText } from 'util';
diff --git a/dist/sandboxes/docker.d.ts b/dist/sandboxes/docker.d.ts
index c7cc9e78dd3f14021ae64061d70e715bb98f768f..683eae5626082d3d0ba1f2ea9391a6be6bd05476 100644
--- a/dist/sandboxes/docker.d.ts
+++ b/dist/sandboxes/docker.d.ts
@@ -14,6 +14,14 @@ export { d as defaultImageName } from '../mountUtils-CCA-bbpK.js';
 interface DockerOptions {
     /** Docker image name (default: derived from repo directory name). */
     readonly imageName?: string;
+    /**
+     * runcastle patch — the container's `--name`, chosen by the caller.
+     *
+     * A caller that names the container can kill it directly (`docker rm -f
+     * <name>`) without first discovering it. When omitted, sandcastle's own
+     * `sandcastle-<uuid>` name is used, exactly as before.
+     */
+    readonly containerName?: string;
     /**
      * The UID of the `agent` user inside the container image (default: host UID via `process.getuid()`, or 1000).
      *
@@ -36,10 +44,12 @@ interface DockerOptions {
      */
     readonly selinuxLabel?: SelinuxLabel;
     /**
-     * Additional host directories to bind-mount into the sandbox.
+     * Additional host directories or named volumes to mount into the sandbox.
      *
-     * Each entry specifies a `hostPath` (tilde-expanded) and `sandboxPath`.
-     * If `hostPath` does not exist, sandbox creation fails with a clear error.
+     * Each entry specifies a `sandboxPath` plus EITHER a `hostPath`
+     * (tilde-expanded) or, for an engine-managed named volume, a `volume`.
+     * If `hostPath` does not exist, sandbox creation fails with a clear error;
+     * a `volume` is created on demand by the engine and is never checked.
      */
     readonly mounts?: readonly MountConfig[];
     /** Environment variables injected by this provider. Merged at launch time with env resolver and agent provider env. */
diff --git a/dist/sandboxes/no-sandbox.d.ts b/dist/sandboxes/no-sandbox.d.ts
index 282726ed9d3f1310a4fc4a66985e8db2b917a045..67aa5809857f37ad437c39e37e56cd72b8cece25 100644
--- a/dist/sandboxes/no-sandbox.d.ts
+++ b/dist/sandboxes/no-sandbox.d.ts
@@ -16,6 +16,15 @@ import { N as NoSandboxProvider } from '../SandboxProvider-EkSMuBp8.js';
 interface NoSandboxOptions {
     /** Environment variables injected by this provider. Merged at launch time. */
     readonly env?: Record<string, string>;
+    /**
+     * runcastle patch — called with the PID of every child this provider spawns.
+     *
+     * The host provider spawns a fresh shell per `exec`, so a callback is the
+     * only way a caller can learn what to kill when a run is aborted. Not called
+     * when the spawn itself failed and there is no PID. When omitted, nothing
+     * changes.
+     */
+    readonly onChildSpawn?: (pid: number) => void;
     /**
      * Maximum number of characters of streamed `exec` output retained per stream
      * (stdout and stderr) when an `onLine` callback is supplied (default: 64KiB).
diff --git a/dist/sandboxes/podman.d.ts b/dist/sandboxes/podman.d.ts
index 61e7b4832b63d2859839f3d68ac2af3cbc5d16e0..9bb26af38fa173abe340479370e03620e41fda3c 100644
--- a/dist/sandboxes/podman.d.ts
+++ b/dist/sandboxes/podman.d.ts
@@ -46,10 +46,12 @@ interface PodmanOptions {
      */
     readonly containerGid?: number;
     /**
-     * Additional host directories to bind-mount into the sandbox.
+     * Additional host directories or named volumes to mount into the sandbox.
      *
-     * Each entry specifies a `hostPath` (tilde-expanded) and `sandboxPath`.
-     * If `hostPath` does not exist, sandbox creation fails with a clear error.
+     * Each entry specifies a `sandboxPath` plus EITHER a `hostPath`
+     * (tilde-expanded) or, for an engine-managed named volume, a `volume`.
+     * If `hostPath` does not exist, sandbox creation fails with a clear error;
+     * a `volume` is created on demand by the engine and is never checked.
      */
     readonly mounts?: readonly MountConfig[];
     /** Environment variables injected by this provider. Merged at launch time with env resolver and agent provider env. */
