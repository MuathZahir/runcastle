import { DimLine } from '../../ui'
import type { SettingsPageProps } from './types'

/**
 * Placeholder for the models page — the roster, the default model and the per-step table
 * land in this file next. The shell and the shared setting row are already
 * here, so filling it in is a change to this file alone.
 */
export function ModelsPage(_props: SettingsPageProps) {
  return <DimLine>this page is not built yet.</DimLine>
}
